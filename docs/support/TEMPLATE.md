<!-- Filename: use lowercase-kebab-case.md — no spaces, no question marks (e.g. llm-route-chat-type-error.md) -->
---
title: Descriptive noun phrase — not a question (e.g. "Resolving the X error when Y")
content_type: support
description: One sentence describing what the article covers.
products:
  - gateway
works_on:
  - on-prem
  - konnect
min_version:
  gateway: '3.x'
related_resources:
  - text: Link text
    url: /path/to/page/
tldr:
  q: What question does this article answer?
  a: |
    A concise answer. Use `backticks` for inline code.
---

<!--
TEMPLATE INSTRUCTIONS — delete this block before submitting

title              Noun phrase, not a question. Be specific about the error or scenario.
description        One sentence. Appears in search results.
products           Valid values: gateway, konnect, mesh, insomnia, deck
works_on           Valid values: on-prem, konnect. Required when products includes "gateway".
min_version        Add only if the fix applies to a specific version and later. Verify before adding.
related_resources  Remove this block entirely if you have no links. Never leave /path/to/ placeholders.
tldr               Optional but recommended. Use backticks for code in the answer.
-->

## Root cause

Explain why this error or problem occurs.

## Solution

Step-by-step instructions to resolve the issue.

1. First step.
2. Second step.

```bash
curl -X POST http://localhost:8001/endpoint
```

## Validation

Show how to confirm the solution worked.

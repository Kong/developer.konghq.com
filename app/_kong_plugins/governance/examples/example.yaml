description: 'TO DO' # Used for metadata and search; don't use markdown formatting

# extended_description: '' # Displays on the page; can use any markdown formatting

title: 'TO DO'

weight: 900

# requirements: <- not required
# - "some req"

# variables: <- not required
#   my-region:
#     description: ''
#     value: us

config:
  placeholder: "TO-DO"

min_version:
  gateway: '3.16'

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform
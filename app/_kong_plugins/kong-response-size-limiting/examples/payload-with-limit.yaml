description: 'Blocks upstream responses with a body size greater than 128 megabytes.'

title: Block responses over 128MB

requirements:
  - "The Kong Response Size Limiting plugin is [installed](/plugins/kong-response-size-limiting/#install-the-kong-response-size-limiting-plugin)."

weight: 900

config:
  allowed_payload_size: 128

tools:
  - deck
  - admin-api
  - kic
  - terraform
title: 'Block common bots'

description: Block common bots using built-in regex.
extended_description: |
  This configuration blocks [common bots](https://github.com/Kong/kong/blob/master/kong/plugins/bot-detection/rules.lua) with built-in regex.
  `User_Agent` headers in requests that match are blocked with a `403` error.

weight: 900

config: 
  allow: []
  deny: []

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

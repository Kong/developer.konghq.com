description: Configure the plugin to add a new header to the request before proxying it to the upstream server.

title: Add a new header

weight: 900

config:
  add:
    headers:
      - "New-Header:Header Value"      

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

title: 'Sliding window 5 requests per minute'
description: Configure the plugin to set a rate limit of 5 requests per minute.

weight: 900

config: 
  window_size: [60]
  window_type: sliding
  limit: [5]
  namespace: example_namespace

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

description: 'Block requests if a specific header exists in the request.'

title: 'Trigger plugin on header'

weight: 900

config:
  status_code: 401
  message: '"Error - Authentication required"'
  trigger: ${trigger}

variables:
  trigger:
    value: $X-MY-HEADER

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

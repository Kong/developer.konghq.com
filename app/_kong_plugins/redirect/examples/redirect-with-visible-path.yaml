description: 'Redirect incoming requests to a target location and expose it in the URL.'

title: 'Redirect with visible URL'

weight: 900

variables:
  location:
    value: $YOUR_REDIRECT_URL

config:
  location: ${location}
  keep_incoming_path: true

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

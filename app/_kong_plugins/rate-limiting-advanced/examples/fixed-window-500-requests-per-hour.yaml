title: Fixed window 500 reqs per hour
description: |
  A fixed limit of 500 requests per hour resetting sharply on the hour, ensuring no user can exceed this limit.

weight: 800

config:
  limit: 
    - 500
  window_size: 
    - 3600
  window_type: fixed
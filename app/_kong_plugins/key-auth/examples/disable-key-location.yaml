description: 'This example disables passing the API key through a query parameter when using the Key Auth plugin.'

title: 'Disable API key in query parameters'

weight: 900

config: 
  key_names: 
  - apikey
  key_in_query: false

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

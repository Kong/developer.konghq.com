description: |
  Configure the plugin with a template to use for completions routes.

title: 'Completion route template'

weight: 900

config:     
  templates:
    - name: "developer-prompt"
      template:  |-
        {
            "prompt": "You are a {{language}} programming language expert. Write me a {{program}} program."
        }

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

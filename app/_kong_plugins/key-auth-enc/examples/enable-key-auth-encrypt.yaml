description: 'This example enables the plugin and uses `apikey` for the key name. For an end-to-end tutorial of this example, see [Enable key authentication on a Gateway Service with {{site.base_gateway}}](/how-to/authenticate-consumers-with-key-auth-enc/).'

title: 'Enable Key Auth Encrypted'

weight: 900

requirements: 
 - "Before configuring this plugin, you must [enable {{site.base_gateway}}'s encryption Keyring](/gateway/keyring/#enable-keyring). "

config: 
  key_names: 
  - apikey

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform
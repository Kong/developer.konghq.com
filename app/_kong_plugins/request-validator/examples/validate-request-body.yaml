description: Configure the plugin to use the `kong` validator to check that the request body contains a `name` field with a string value.

title: Validate a request body

weight: 900

config: 
  version: kong
  body_schema:
    name:
      type: string
      required: true

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

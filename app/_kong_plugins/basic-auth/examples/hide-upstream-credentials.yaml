title: 'Hide upstream service credentials'
description: 'Hides the credentials from the upstream service.'

weight: 900

# requirements: <- not required
# - "some req"

# variables: <- not required
#   my-region:
#     description: ''
#     value: us

config:
  hide_credentials: true
description: '' # Used for metadata and search; don't use markdown formatting

# extended_description: '' # Displays on the page; can use any markdown formatting

title: ''

weight: 900

# requirements: <- not required
# - "some req"

# variables: <- not required
#   my-region:
#     description: ''
#     value: us

config: # plugin config in yaml format goes here

min_version:
  gateway: '3.12'

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform
description: "This configuration allows you to specify the JWT token in a cookie."

title: 'Include in cookie'

weight: 900

requirements: 
- "Because this is an auth plugin, you need to create a [Consumer](/gateway/entities/consumer/) and attach a credential to it in the `jwt_secrets` config."

# variables: <- not required
#   my-region:
#     description: ''
#     value: us

config: 
  cookie_names:
  - jwt

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform
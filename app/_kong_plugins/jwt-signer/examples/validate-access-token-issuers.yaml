description: Validates the not bofore claim in an access token.

title: 'Validate the not before claim in an access token'
min_version: 3.12 
weight: 900

config:
  verify_access_token_notbefore: true

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform
description: 'AI Semantic Response Guard'

title: 'Permit or block prompts based on semantic similarity to known LLM responses, preventing misuse of llm/v1/chat or llm/v1/completions requests'

weight: 900

# requirements: <- not required
# - "some req"

# variables: <- not required
#   my-region:
#     description: ''
#     value: us

config: # plugin config in yaml format goes here

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform
description: |
  Configure the plugin to call a Protobuf definition named `hello.proto`.
  For a full how-to guide, see [Use the gRPC-Web plugin to proxy HTTP requests to a gRPC service](/how-to/use-grpc-web).

title: Use a Protobuf definition

weight: 900

requirements: 
  - You have created a Protobuf definition

config: 
  proto: path/to/hello.proto

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

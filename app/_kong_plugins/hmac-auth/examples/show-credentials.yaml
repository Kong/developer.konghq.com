description: 'Shows credentials to the upstream service.'

title: 'Show credentials'

weight: 900

requirements: 
 - You have created a [Consumer with HMAC credentials](/plugins/hmac-auth/#consumer-authentication)


config:
  hide_credentials: false

tools:
  - deck
  - admin-api
  - konnect-api
  - kic
  - terraform

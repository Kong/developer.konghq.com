---
title: Library Kong uses to render API specs in the on-prem Developer Portal
content_type: support
published: false
description: "Kong uses a fork of the `swagger-ui` code to render API specs in the on-prem Developer Portal."
products:
  - gateway
works_on:
  - on-prem
  - konnect
tldr:
  q: What library does Kong use in its on-prem Developer Portal to render API specs?
  a: |
    Kong renders API specs in the on-prem Developer Portal using a fork of `swagger-ui`.
related_resources:
  - text: "`Kong/swagger-ui`"
    url: "https://github.com/Kong/swagger-ui"
---

## Library used to render API specs

Kong uses a fork of the `swagger-ui` code which is available on GitHub.

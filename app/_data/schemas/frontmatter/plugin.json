{
    "$id": "schema:plugin",
    "allOf": [{ "$ref": "schema:base" }],
    "properties": {
        "min_version": {
          "type": "object"
        }
    },
    "required": ["content_type", "title", "description", "products", "works_on"]
  }
---
messages:
  - ID: D100
    Severity: error
    Description: |
      Plugin `jq` is not available in Kong Gateway versions < 2.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [jq plugin](/plugins/jq/)
  - ID: D101
    Severity: error
    Description: |
      For the `canary` plugin, one or more of the following `config` fields are set:
      `hash_header` but Kong Gateway versions < 2.6.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Canary plugin](/plugins/canary/)
  - ID: D102
    Severity: error
    Description: |
      For the `canary` plugin, `config.hash` field has been set to `header`. This is
      not supported in Kong Gateway versions < 2.6.0.0. The plugin configuration has
      been changed to `config.hash=consumer` in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Canary plugin](/plugins/canary/)
  - ID: D103
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `authentication`, `keepalive_enabled`, `security` but Kong Gateway versions
      < 2.6.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D104
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `authentication`, `keepalive_enabled`, `security` but Kong Gateway
      versions < 2.6.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D105
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, either `config.strategy` is set to
      `local` or `config.identifier` is set to `path` or both. These settings are not
      supported in Kong Gateway versions < 2.6.0.0. In the data plane node, the plugin
      configuration has one or more of the following changes: (a) `config.strategy`
      has been updated to `redis`, (b) `config.sync_rate` has been updated to `1`, (c)
      `config.identifier has been updated to `consumer`.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D106
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following unsupported
      `config` fields or values for a `config` field are in use:
      `by_username_ignore_case`, `disable_session`,
      `downstream_introspection_jwt_header`, `downstream_user_info_jwt_header`,
      `introspection_accept`, `introspection_check_active`,
      `upstream_introspection_jwt_header`, `upstream_user_info_jwt_header`,
      `userinfo_accept`, `userinfo_headers_client`, `userinfo_headers_names`,
      `userinfo_headers_values`, `userinfo_query_args_client`,
      `userinfo_query_args_names`, `userinfo_query_args_values`, `auth_methods` array
      field with value `userinfo`, `ignore_signature` array field with value
      `introspection`, `ignore_signature` array field with value `userinfo`,
      `login_methods` array field with value `userinfo`, `token_headers_grants` array
      field with value `refresh_token`.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D107
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `path` but Kong Gateway versions < 2.6.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D108
    Severity: error
    Description: |
      For the `forward-proxy` plugin, one or more of the following `config` fields are
      set: `auth_username`, `auth_password` but Kong Gateway versions < 2.7.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Forward Proxy plugin](/plugins/forward-proxy/)
  - ID: D109
    Severity: error
    Description: |
      For the `mocking` plugin, one or more of the following `config` fields are set:
      `random_examples` but Kong Gateway versions < 2.7.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Mocking plugin](/plugins/mocking/)
  - ID: D110
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `enforce_consumer_groups`, `consumer_groups` but Kong Gateway
      versions < 2.7.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D111
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `redis.username`, `redis.sentinel_username` but Kong Gateway
      versions < 2.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D112
    Severity: error
    Description: |
      For the `proxy-cache-advanced` plugin, one or more of the following `config`
      fields are set: `redis.username`, `redis.sentinel_username` but Kong Gateway
      versions < 2.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D113
    Severity: error
    Description: |
      For the `canary` plugin, one or more of the following `config` fields are set:
      `canary_by_header_name` but Kong Gateway versions < 2.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Canary plugin](/plugins/canary/)
  - ID: D114
    Severity: error
    Description: |
      For the `forward-proxy` plugin, one or more of the following `config` fields are
      set: `https_proxy_host`, `https_proxy_port` but Kong Gateway versions < 2.8.0.0
      do not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Forward Proxy plugin](/plugins/forward-proxy/)
  - ID: D115
    Severity: warning
    Description: |
      N/A
    Resolution: |
      N/A
    DocumentationURL: |
      [Forward Proxy plugin](/plugins/forward-proxy/)
  - ID: D116
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `session_redis_username`, `resolve_distributed_claims` but Kong Gateway
      versions < 2.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D117
    Severity: warning
    Description: |
      N/A
    Resolution: |
      N/A
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D118
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `cluster_name` but Kong Gateway versions < 2.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D119
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `cluster_name` but Kong Gateway versions < 2.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D120
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following fields:
      `authentication.mechanism` have been set to one or more of the following values:
      `SCRAM-SHA-512` but Kong Gateway versions < 2.8.1.1 do not support these values.
      Impacted fields have been updated to the following values in the data plane
      node: `SCRAM-SHA-256`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.1.1` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D121
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following fields:
      `authentication.mechanism` have been set to one or more of the following values:
      `SCRAM-SHA-512` but Kong Gateway versions < 2.8.1.1 do not support these values.
      Impacted fields have been updated to the following values in the data plane
      node: `SCRAM-SHA-256`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.1.1` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D122
    Severity: error
    Description: |
      For the `mtls-auth` plugin, one or more of the following `config` fields are
      set: `http_proxy_host`, `http_proxy_port`, `https_proxy_host`,
      `https_proxy_port` but Kong Gateway versions < 2.8.1.1 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.1.1` or above.
    DocumentationURL: |
      [mTLS Authentication plugin](/plugins/mtls-auth/)
  - ID: D123
    Severity: error
    Description: |
      It seems you are using Konnect Developer Portal with Kong Gateway. For the
      Developer Portal's application registration feature to work correctly,
      `konnect-application-auth` plugin is required but this plugin is not available
      on Kong Gateway versions < 3.0.0.0 The feature is not working as intended and
      can cause severe security issues including authentication and authorization not
      being enforced in the Gateway.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D124
    Severity: error
    Description: |
      For the `ldap-auth-advanced` plugin, one or more of the following `config`
      fields are set: `groups_required` but Kong Gateway versions < 3.0.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [LDAP Authentication Advanced plugin](/plugins/ldap-auth-advanced/)
  - ID: D125
    Severity: error
    Description: |
      For the `opa` plugin, one or more of the following `config` fields are set:
      `include_body_in_opa_input`, `include_parsed_json_body_in_opa_input`,
      `ssl_verify` but Kong Gateway versions < 3.0.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [OPA plugin](/plugins/opa/)
  - ID: D126
    Severity: error
    Description: |
      For the `statsd-advanced` plugin, one or more of the following `config` fields
      are set: `consumer_identifier_default`, `service_identifier_default`,
      `workspace_identifier_default` but Kong Gateway versions < 3.0.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D127
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, `config.sync_rate` was set to a float
      value less than 1.0. This configuration is not supported in Kong Gateway
      versions < 2.6.0.0. The configuration has been changed with `config.sync_rate`
      set to value of `1`.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D128
    Severity: error
    Description: |
      For the `openid-connect` plugin, `config.anonymous` field has been set to a
      string value that is not a UUID, possibly to reference a consumer via username.
      This feature is not supported in Kong Gateway versions < 3.0.0.0. The plugin
      configuration has been changed to remove the `config.anonymous` field.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D129
    Severity: error
    Description: |
      Konnect cloud's Developer Portal's application registration workflow requires
      features introduced in Kong Gateway 3.0.0.0. Some features of the Developer
      Portal are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D130
    Severity: error
    Description: |
      For the `Plugin` entity, the `ordering` field has been set but Kong Gateway
      versions < 3.0 do not support plugin re-ordering. The `ordering` field has been
      removed from the configuration; plugins are executing based on the priorities
      coded into plugins.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Plugins entity](/gateway/entities/plugin/)
  - ID: D131
    Severity: error
    Description: |
      For the `service` entity, `protocol` field has been set to `ws` or `wss`. This
      is not supported in Kong Gateway versions < 3.0.0.0. The service and all
      associated route(s) and plugin(s) have been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Services entity](/gateway/entities/service/)
  - ID: D132
    Severity: error
    Description: |
      For the `route` entity or associated `service`, `protocol` field has been set to
      `ws` or `wss`. This is not supported in Kong Gateway versions < 3.0.0.0. The
      route and all associated plugin(s) have been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: D133
    Severity: error
    Description: |
      For the `plugin` entity, `protocol` field has been set to `ws` or `wss`. This is
      not supported in Kong Gateway versions < 3.0.0.0. The plugin has been removed in
      the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Plugins entity](/gateway/entities/plugin/)
  - ID: D134
    Severity: error
    Description: |
      Plugin `websocket-validator` is not available in Kong Gateway versions <
      3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Websocket Validator plugin](/plugins/websocket-validator/)
  - ID: D135
    Severity: error
    Description: |
      Plugin `websocket-size-limit` is not available in Kong Gateway versions <
      3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Websocket Size Limit plugin](/plugins/websocket-size-limit/)
  - ID: D136
    Severity: error
    Description: |
      Plugin `tls-handshake-modifier` is not available in Kong Gateway versions <
      3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [TLS Handshake Modifier plugin](/plugins/tls-handshake-modifier/)
  - ID: D137
    Severity: error
    Description: |
      Plugin `tls-metadata-headers` is not available in Kong Gateway versions <
      3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [TLS Metadata Headers plugin](/plugins/tls-metadata-headers/)
  - ID: D138
    Severity: error
    Description: |
      For the `forward-proxy` plugin, one or more of the following `config` fields are
      set: `x_headers` but Kong Gateway versions < 3.1.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Forward Proxy plugin](/plugins/forward-proxy/)
  - ID: D139
    Severity: error
    Description: |
      For the `mtls-auth` plugin, one or more of the following `config` fields are
      set: `send_ca_dn` but Kong Gateway versions < 3.1.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [mTLS Authentication plugin](/plugins/mtls-auth/)
  - ID: D140
    Severity: error
    Description: |
      For the `route-transformer-advanced` plugin `config.escape_path` is set. This is
      not supported in Kong Gateway versions < 2.8.2.0 and in Kong Gateway versions >
      3.0.x.x. Plugin features that rely on this field are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.2.0` or above, excluding versions
      `3.0.0.x` and `3.0.1.x`
    DocumentationURL: |
      [Route Transformer Advanced plugin](/plugins/route-transformer-advanced/)
  - ID: D141
    Severity: error
    Description: |
      Plugin `app-dynamics` is not available in Kong Gateway versions < 3.1.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [App Dynamics plugin](/plugins/app-dynamics/)
  - ID: D142
    Severity: error
    Description: |
      Plugin `saml` is not available in Kong Gateway versions < 3.1.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D143
    Severity: error
    Description: |
      Plugin `jwe-decrypt` is not available in Kong Gateway versions < 3.1.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Kong JWE Decrypt plugin](/plugins/jwe-decrypt/)
  - ID: D144
    Severity: error
    Description: |
      Plugin `xml-threat-protection` is not available in Kong Gateway versions <
      3.1.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [XML Threat Protection plugin](/plugins/xml-threat-protection/)
  - ID: D145
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `error_code`, `error_message`, `disable_penalty` but Kong
      Gateway versions < 3.1.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D146
    Severity: error
    Description: |
      For the `vaults` entity, one or more of the following schema fields are set:
      `auth_method`, `kube_role`, `kube_api_token_file` but Kong Gateway versions <
      3.1.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: D147
    Severity: error
    Description: |
      The `consumer_group` entity is being used, but Kong Gateway versions < 2.8.0.0
      do not support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Consumer Groups entity](/gateway/entities/consumer-group/)
  - ID: D148
    Severity: error
    Description: |
      As part of the `Consumer Groups` feature, some Consumers have been set to belong
      to one or more of such groups but Kong Gateway versions < 2.8 do not support
      Consumer Groups. The `group` membership for consumers has been removed from the
      configuration.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Consumer Groups entity](/gateway/entities/consumer-group/)
  - ID: D149
    Severity: error
    Description: |
      As part of the `Consumer Groups` feature, some Rate Limiting Advanced overrides
      have been to one of such groups but Kong Gateway versions < 2.8 do not support
      Consumer Groups. These `overrides` have been removed from the configuration;
      Rate Limiting policies are governed by global configuration.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Consumer Groups entity](/gateway/entities/consumer-group/)
  - ID: D150
    Severity: error
    Description: |
      For the `mocking` plugin, one or more of the following `config` fields are set:
      `included_status_codes`, `random_status_code` but Kong Gateway versions <
      3.1.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Mocking plugin](/plugins/mocking/)
  - ID: D151
    Severity: error
    Description: |
      For the `oauth2-introspection` plugin, `config.anonymous` field has been set to
      a string value that is not a UUID, possibly to reference a consumer via
      username. This feature is not supported in Kong Gateway versions < 3.1.0.0. The
      plugin configuration has been changed to remove the `config.anonymous` field.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [OAuth 2.0 Introspection plugin](/plugins/oauth2-introspection/)
  - ID: D152
    Severity: error
    Description: |
      For the `mtls-auth` plugin, `config.anonymous` field has been set to a string
      value that is not a UUID, possibly to reference a consumer via username. This
      feature is not supported in Kong Gateway versions < 3.1.0.0. The plugin
      configuration has been changed to remove the `config.anonymous` field.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [mTLS Authentication plugin](/plugins/mtls-auth/)
  - ID: D153
    Severity: error
    Description: |
      For the `ldap-auth-advanced` plugin, `config.anonymous` field has been set to a
      string value that is not a UUID, possibly to reference a consumer via username.
      This feature is not supported in Kong Gateway versions < 3.1.0.0. The plugin
      configuration has been changed to remove the `config.anonymous` field.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [LDAP Authentication Advanced plugin](/plugins/ldap-auth-advanced/)
  - ID: D154
    Severity: error
    Description: |
      For the `request-transformer-advanced` plugin, one or more of the following
      `config` fields are set: `dots_in_keys`, `replace.json_types`,
      `append.json_types`, `add.json_types` but Kong Gateway versions < 3.1.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Request Transformer Advanced plugin](/plugins/request-transformer-advanced/)
  - ID: D156
    Severity: warning
    Description: |
      For the `openid-connect` plugin, `config.authorization_cookie_same_site` has
      been set to `Default`. This is not supported in Kong Gateway versions < 3.2.0.0.
      The plugin configuration has been changed to
      `config.authorization_cookie_same_site=Lax` in the data plane node (default
      value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D157
    Severity: warning
    Description: |
      For the `openid-connect` plugin, `config.session_cookie_same_site` has been set
      to `Default`. This is not supported in Kong Gateway versions < 3.2.0.0. The
      plugin configuration has been changed to the
      `config.session_cookie_same_site=Lax` in the data plane node (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D158
    Severity: warning
    Description: |
      For the `openid-connect` plugin, one or more `config` fields are set but Kong
      Gateway versions < 3.2.0.0 do not support these fields. The plugin configuration
      has been updated and these fields have been mapped to their old names:
      `session_memcached_port` => `session_memcache_port`,
      `authorization_cookie_http_only` => `authorization_cookie_httponly`,
      `session_rolling_timeout` => `session_cookie_lifetime`,
      `session_cookie_same_site` => `session_cookie_samesite`,
      `session_memcached_prefix` => `session_memcache_prefix`,
      `session_memcached_socket` => `session_memcache_socket`,
      `session_memcached_host` => `session_memcache_host`,
      `authorization_rolling_timeout` => `authorization_cookie_lifetime`,
      `authorization_cookie_same_site` => `authorization_cookie_samesite`,
      `session_idling_timeout` => `session_cookie_idletime`,
      `session_cookie_http_only` => `session_cookie_httponly`,
      `session_redis_cluster_max_redirections` => `session_redis_cluster_maxredirections`
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D159
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `session_audience`, `session_remember`, `session_remember_cookie_name`,
      `session_remember_rolling_timeout`, `session_remember_absolute_timeout`,
      `session_absolute_timeout`, `session_store_metadata`,
      `session_response_headers`, `session_request_headers`,
      `session_enforce_same_subject`, `session_hash_subject`,
      `session_hash_storage_key` but Kong Gateway versions < 3.2.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D160
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `session_cookie_renew`, `session_cookie_maxsize`, `session_strategy`,
      `session_compressor` but Kong Gateway versions >= 3.2.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please remove these fields from the plugin configuration.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D161
    Severity: warning
    Description: |
      For the `openid-connect` plugin, `config.session_storage` field has been set to
      `memcached`. This is not supported in Kong Gateway versions < 3.2.0.0. The
      plugin configuration has been changed to `config.session_storage=memcache` in
      the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D162
    Severity: error
    Description: |
      For the `saml` plugin, one or more of the following `config` fields are set:
      `session_audience`, `session_remember`, `session_remember_cookie_name`,
      `session_remember_rolling_timeout`, `session_remember_absolute_timeout`,
      `session_store_metadata`, `session_response_headers`, `session_request_headers`,
      `session_enforce_same_subject`, `session_hash_subject`,
      `session_hash_storage_key` but Kong Gateway versions < 3.2.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D163
    Severity: warning
    Description: |
      For the `saml` plugin, one or more `config` fields are set but Kong Gateway
      versions < 3.2.0.0 do not support these fields. The plugin configuration has
      been updated and these fields have been mapped to their old names:
      `session_rolling_timeout` => `session_cookie_lifetime`,
      `session_cookie_http_only` => `session_cookie_httponly`,
      `session_memcached_prefix` => `session_memcache_prefix`,
      `session_redis_cluster_max_redirections` => `session_redis_cluster_maxredirections`,
      `session_idling_timeout` => `session_cookie_idletime`,
      `session_cookie_same_site` => `session_cookie_samesite`,
      `session_memcached_socket` => `session_memcache_socket`,
      `session_memcached_host` => `session_memcache_host`,
      `session_memcached_port` => `session_memcache_port`
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D164
    Severity: error
    Description: |
      For the `saml` plugin, one or more of the following `config` fields are set:
      `session_cookie_renew`, `session_auth_ttl`, `session_cookie_maxsize`,
      `session_strategy`, `session_compressor` but Kong Gateway versions >= 3.2.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please remove these fields from the plugin configuration.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D165
    Severity: warning
    Description: |
      For the `saml` plugin, `config.session_storage` field has been set to
      `memcached`. This is not supported in Kong Gateway versions < 3.2.0.0. The
      plugin configuration has been changed to `config.session_storage=memcache` in
      the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D166
    Severity: warning
    Description: |
      For the `saml` plugin, `config.session_cookie_same_site` has been set to
      `Default`. This is not supported in Kong Gateway versions < 3.2.0.0. The plugin
      configuration has been changed to `config.session_cookie_same_site=Lax` in the
      data plane node (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D167
    Severity: error
    Description: |
      For the `app-dynamics` plugin, instances have been configured on a specific
      consumer. This is not supported in Kong Gateway versions < 3.2.0.0. The plugin
      has been removed in the data plane node.
    Resolution: |
      Unset the `consumer` fields for the plugin, or upgrade Kong Gateway to a version >=
      3.2.0.0.
    DocumentationURL: |
      [App Dynamics plugin](/plugins/app-dynamics/)
  - ID: D168
    Severity: error
    Description: |
      Plugin `oas-validation` is not available in Kong Gateway versions < 3.1.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [OAS Validation plugin](/plugins/oas-validation/)
  - ID: D169
    Severity: error
    Description: |
      For the `service` entity, one of `tls_verify`, `tls_verify_depth`,
      `ca_certificates` or `client_certificate` fields has been set with `protocol`
      set to `TLS`. This is not supported in Kong Gateway versions < 3.2.0.0. These
      fields have been removed from the service configuration in the data plane node
      and the service has been disabled.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Services entity](/gateway/entities/service/)
  - ID: D170
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, `config.strategy` field has been set to
      `cluster`. This is not supported in Kong Gateway versions < 3.2.0.0. The plugin
      configuration has been changed to `config.strategy=local` and `sync_rate=null`
      in the data plane node and might not work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D171
    Severity: error
    Description: |
      For the `service` entity, one of `tls_verify`, `tls_verify_depth`,
      `ca_certificates` or `client_certificate` fields has been set with `protocol`
      set to `TLS`. This is not supported in Kong Gateway versions < 3.2.0.0. These
      services and the associated plugins and routes have been removed from the
      configuration in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Services entity](/gateway/entities/service/)
  - ID: D172
    Severity: error
    Description: |
      For the `opa` plugin, one or more of the following `config` fields are set:
      `include_uri_captures_in_opa_input` but Kong Gateway versions < 3.1.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [OPA plugin](/plugins/opa/)
  - ID: D173
    Severity: error
    Description: |
      For the `degraphql` plugin, one or more of the following `config` fields are
      set: `graphql_server_path` but Kong Gateway versions < 3.0.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [DeGraphQL plugin](/plugins/degraphql/)
  - ID: D174
    Severity: error
    Description: |
      The degraphql plugin has been disabled because of known issues on Kong Gateway <
      3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [DeGraphQL plugin](/plugins/degraphql/)
  - ID: D175
    Severity: error
    Description: |
      The deGraphQL Route entities have been removed because of known issues with Kong
      Gateway < 3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [DeGraphQL plugin](/plugins/degraphql/)
  - ID: D176
    Severity: error
    Description: |
      For the `jwt-signer` plugin, one or more of the following `config` fields are
      set: `add_claims`, `set_claims` but Kong Gateway versions < 3.3.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [JWT Signer plugin](/plugins/jwt-signer/)
  - ID: D177
    Severity: error
    Description: |
      For the `proxy-cache-advanced` plugin, one or more of the following `config`
      fields are set: `ignore_uri_case` but Kong Gateway versions < 3.3.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D178
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, instances have been configured on a
      specific consumer_group. This is not supported in Kong Gateway versions <
      3.4.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.4.0.0.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D179
    Severity: error
    Description: |
      For the `request-transformer-advanced` plugin, instances have been configured on
      a specific consumer_group. This is not supported in Kong Gateway versions <
      3.4.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.4.0.0.
    DocumentationURL: |
      [Request Transformer Advanced plugin](/plugins/request-transformer-advanced/)
  - ID: D180
    Severity: error
    Description: |
      For the `response-transformer-advanced` plugin, instances have been configured
      on a specific consumer_group. This is not supported in Kong Gateway versions <
      3.4.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.4.0.0.
    DocumentationURL: |
      [Response Transformer Advanced plugin](/plugins/response-transformer-advanced/)
  - ID: D181
    Severity: error
    Description: |
      For the `request-transformer` plugin, instances have been configured on a
      specific consumer_group. This is not supported in Kong Gateway versions <
      3.4.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.4.0.0.
    DocumentationURL: |
      [Request Transformer plugin](/plugins/request-transformer/)
  - ID: D182
    Severity: error
    Description: |
      For the `response-transformer` plugin, instances have been configured on a
      specific consumer_group. This is not supported in Kong Gateway versions <
      3.4.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.4.0.0.
    DocumentationURL: |
      [Response Transformer plugin](/plugins/response-transformer/)
  - ID: D183
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `expose_error_code` but Kong Gateway versions < 3.4.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D184
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `custom_fields_by_lua` but Kong Gateway versions < 3.4.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D185
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `token_cache_key_include_scope` but Kong Gateway versions < 3.4.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D186
    Severity: error
    Description: |
      For the `forward-proxy` plugin, one or more of the following `config` fields are
      set in a reference format: `auth_username`, `auth_password` but Konnect only
      supports Vaults starting from Kong Gateway version 3.1.0.0. This plugin instance
      will cause the Kong Gateway to fail payload validation, causing payload updates
      to fail and potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Forward Proxy plugin](/plugins/forward-proxy/)
  - ID: D187
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set in a reference format: `authentication.user`, `authentication.password`
      but Konnect only supports Vaults starting from Kong Gateway version 3.1.0.0.
      This plugin instance will cause the Kong Gateway to fail payload validation,
      causing payload updates to fail and potentially getting the data plane into an
      out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D188
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are set
      in a reference format: `authentication.user`, `authentication.password` but
      Konnect only supports Vaults starting from Kong Gateway version 3.1.0.0. This
      plugin instance will cause the Kong Gateway to fail payload validation, causing
      payload updates to fail and potentially getting the data plane into an
      out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D189
    Severity: error
    Description: |
      For the `ldap-auth-advanced` plugin, one or more of the following `config`
      fields are set in a reference format: `ldap_password`, `bind_dn` but Konnect
      only supports Vaults starting from Kong Gateway version 3.1.0.0. This plugin
      instance will cause the Kong Gateway to fail payload validation, causing payload
      updates to fail and potentially getting the data plane into an out-of-sync
      state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [LDAP Authentication Advanced plugin](/plugins/ldap-auth-advanced/)
  - ID: D190
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set in a reference format: `client_id`, `client_secret`, `session_secret`,
      `session_redis_username`, `session_redis_password` but Konnect only supports
      Vaults starting from Kong Gateway version 3.1.0.0. This plugin instance will
      cause the Kong Gateway to fail payload validation, causing payload updates to
      fail and potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D191
    Severity: error
    Description: |
      For the `request-transformer-advanced` plugin, one or more of the following
      `config` fields are set in a reference format: `rename.body`, `rename.headers`,
      `rename.querystring`, `replace.body`, `replace.headers`, `replace.querystring`,
      `add.body`, `add.headers`, `add.querystring`, `append.body`, `append.headers`,
      `append.querystring` but Konnect only supports Vaults starting from Kong Gateway
      version 3.1.0.0. This plugin instance will cause the Kong Gateway to fail
      payload validation, causing payload updates to fail and potentially getting the
      data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Request Transformer Advanced plugin](/plugins/request-transformer-advanced/)
  - ID: D192
    Severity: error
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, instances have been configured
      with `strategy=redis` and the `cluster_addresses` field set. This is not
      supported in Kong Gateway versions < 3.4.1.0. For data plane nodes with version
      < 3.2.0.0, the plugin has been completely removed.
    Resolution: |
      Unset the `cluster_addresses` field for the plugin, or upgrade Kong Gateway to a
      version >= 3.4.1.0.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D193
    Severity: error
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, instances have been configured
      with `strategy=redis` and the `cluster_addresses` field set. This is not
      supported in Kong Gateway versions < 3.4.1.0. For data plane nodes with version >=
      3.2.0.0 and < 3.4.1.0, the plugin configuration has been changed to
      `config.strategy=cluster` and `config.sync_rate=-1`.
    Resolution: |
      Unset the `redis` fields for the plugin, or upgrade Kong Gateway to a version >=
      3.4.1.0.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D194
    Severity: error
    Description: |
      For the `canary` plugin, a configuration `start` time is in the past and will be
      sent to the data plane node(s). This is not supported in Kong Gateway versions <
      3.4.1.0.
    Resolution: |
      Fix the `canary` plugin configuration or upgrade dataplane to 3.4.1.0+.
    DocumentationURL: |
      [Canary plugin](/plugins/canary/)
  - ID: D195
    Severity: error
    Description: |
      For the `openid-connect` plugin, the `using_pseudo_issuer` config field is set,
      but only Kong Gateway versions >= 3.4.1.0 and >= 3.3.1.2 for the 3.3 series
      support this field. Plugin features that rely on this field are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway or remove this field from configuration.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D196
    Severity: error
    Description: |
      For the `proxy-cache-advanced` plugin, one or more of the following `config`
      fields are set: `response_headers` but Kong Gateway versions < 3.5.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D197
    Severity: error
    Description: |
      For the `oas-validation` plugin, one or more of the following `config` fields
      are set: `include_base_path` but Kong Gateway versions < 3.4.3.6 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.6` or above.
    DocumentationURL: |
      [OAS Validation plugin](/plugins/oas-validation/)
  - ID: D198
    Severity: error
    Description: |
      For the `mocking` plugin, one or more of the following `config` fields are set:
      `include_base_path` but Kong Gateway versions < 3.5.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [Mocking plugin](/plugins/mocking/)
  - ID: D199
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `introspection_token_param_name`, `revocation_token_param_name` but
      Kong Gateway versions < 3.5.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D200
    Severity: error
    Description: |
      For the `openid-connect` plugin, the `unauthorized_destroy_session` config field
      is set, but only Kong Gateway versions >=3.4.1.0 and >=2.8.4.5 for the 2.8 serie
      support this field. Plugin features that rely on this field are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway or remove this field from configuration.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D201
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `proof_of_possession_mtls`,
      `proof_of_possession_auth_methods_validation` but Kong Gateway versions <
      3.5.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D202
    Severity: warning
    Description: |
      Instance(s) of Konnect Application Registrations have been configured with the
      field `consumer_groups` set. This is not supported in Kong Gateway versions <
      3.5.0.0. Application Registration is still functioning correctly, but you may
      want to upgrade to gain additional functionality.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D203
    Severity: error
    Description: |
      For the `proxy-cache-advanced` plugin, instances have been configured on a
      specific consumer_group. This is not supported in Kong Gateway versions <
      3.6.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.6.0.0.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D204
    Severity: warning
    Description: |
      Instance(s) of Konnect Application Registration have been configured with one or
      more of the following fields: `application_context`, `auth_strategy_id`, and
      `exhausted_scopes` set. These fields are not supported in Kong Gateway versions
      < 3.6.0.0. Application Registration is still functioning correctly, but you may
      want to upgrade to gain additional functionality.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D205
    Severity: error
    Description: |
      For the `request-validator` plugin, the config field
      `content_type_parameter_validation` has been used. This is not supported in Kong
      Gateway versions older than 3.6.1.5 and versions older than 3.7.1.0 for the
      3.7.x release line.
    Resolution: |
      Please upgrade Kong Gateway to a version that supports this field.
    DocumentationURL: |
      [Request Validator plugin](/plugins/request-validator/)
  - ID: D206
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `tls_client_auth_cert_id`, `tls_client_auth_ssl_verify`,
      `mtls_token_endpoint`, `mtls_introspection_endpoint`, `mtls_revocation_endpoint`
      but Kong Gateway versions < 3.6.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D207
    Severity: error
    Description: |
      The plugin `openid-connect` was configured with mTLS client auth but Kong
      Gateway versions < 3.6.0.0 do not support it. As a result, the entire
      configuration is rejected and Konnect will not update the configuration in the
      Data Plane Node until this defect is corrected.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D208
    Severity: error
    Description: |
      For the `openid-connect` plugin, the `introspection_headers_values` config was
      found to contain a value matching the referenceable field format, but only Kong
      Gateway versions >= 3.5.0.4 support this field. Plugin features that rely on
      this field are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.4` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D209
    Severity: error
    Description: |
      The plugin `konnect-application-auth` was defined at route level but Kong
      Gateway versions < 3.6.0.0 do not support it. As a result, the entire
      configuration is rejected and Konnect will not update the configuration in the
      Data Plane Node until this defect is corrected.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D210
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following
      `login_redirect_uri`, `logout_redirect_uri` and `scopes` fields are set in a
      reference format, but only Kong Gateways falling in the following version ranges
      support reference formats for these fields: versions >= 3.4.3.2 and < 3.5.0.0;
      versions >= 3.5.0.2. These fields have not been modified and will fail
      validation in the data plane. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway or remove fields using reference values.
      Alternatively, update fields using reference values to instead use explicit
      values.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D211
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `pushed_authorization_request_endpoint`,
      `pushed_authorization_request_endpoint_auth_method`,
      `require_pushed_authorization_requests`, `require_proof_key_for_code_exchange`
      but Kong Gateway versions < 3.6.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D212
    Severity: error
    Description: |
      For the `key-auth` plugin, one or more of the following `config` fields are set:
      `ttl` but Kong Gateway versions < 3.4.1.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.1.0` or above.
    DocumentationURL: |
      [Key Auth plugin](/plugins/key-auth/)
  - ID: D213
    Severity: error
    Description: |
      For the `mtls-auth` plugin, one or more of the following `config` fields are
      set: `default_consumer` but Kong Gateway versions < 3.4.3.5 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.5` or above.
    DocumentationURL: |
      [mTLS Authentication plugin](/plugins/mtls-auth/)
  - ID: D214
    Severity: error
    Description: |
      There is a syntax error in one or more route expression.
    Resolution: |
      Rewrite the expression, following the route expression language.
    DocumentationURL: |
      [Expressions Router](/gateway/routing/expressions/)
  - ID: D215
    Severity: error
    Description: |
      A route expression is using one or more fields that are not supported by this
      specific Kong Gateway version.
    Resolution: |
      Update Kong Gateway to a version that supports all used expression fields, or
      rewrite the expressions using only fields supported by your Kong Gateway
      version.
    DocumentationURL: |
      [Expressions Router](/gateway/routing/expressions/#matching-fields)
  - ID: D216
    Severity: error
    Description: |
      For the `oas-validation` plugin, the `api_spec_encoded` config field is set, but
      only Kong Gateway versions >= 3.4.3.6 for the 3.4 series and >= 3.6.1.2 for the
      3.6 series support this field. Plugin features that rely on this field are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway or remove this field from configuration.
    DocumentationURL: |
      [OAS Validation plugin](/plugins/oas-validation/)
  - ID: D217
    Severity: error
    Description: |
      For the `openid-connect` plugin, `config.response_mode` field has been set
      either to `query.jwt`, `form_post.jwt`, `fragment.jwt` or `jwt`. This is not
      supported in Kong Gateway versions < 3.7.0.0. The plugin configuration has been
      changed to `config.response_mode=query` in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D218
    Severity: error
    Description: |
      Plugin `ai-azure-content-safety` is not available in Kong Gateway versions <
      3.7.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Azure Content Safety plugin](/plugins/ai-azure-content-safety/)
  - ID: D219
    Severity: error
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, `config.strategy` field has been
      set to `redis`. This is not supported in Kong Gateway versions < 3.7.0.0. The
      plugin configuration has been changed to `config.strategy=memory` in the data
      plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D220
    Severity: error
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, one or more of the following
      `config` fields are set: `redis` but Kong Gateway versions < 3.7.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D221
    Severity: error
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, one or more of the following
      `config` fields are set: `bypass_on_err` but Kong Gateway versions < 3.7.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D222
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `proof_of_possession_dpop`, `dpop_use_nonce`, `dpop_proof_lifetime` but
      Kong Gateway versions < 3.7.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D223
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `require_signed_request_object` but Kong Gateway versions < 3.7.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D224
    Severity: error
    Description: |
      Plugin `ai-rate-limiting-advanced` is not available in Kong Gateway versions <
      3.7.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D225
    Severity: error
    Description: |
      For the `oas-validation` plugin, one or more of the following `config` fields
      are set: `custom_base_path` but Kong Gateway versions < 3.7.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [OAS Validation plugin](/plugins/oas-validation/)
  - ID: D226
    Severity: error
    Description: |
      For the `mocking` plugin, one or more of the following `config` fields are set:
      `custom_base_path` but Kong Gateway versions < 3.7.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Mocking plugin](/plugins/mocking/)
  - ID: D227
    Severity: error
    Description: |
      For the `konnect-application-auth` plugin, one or more of the following `config`
      fields are set: `v2_strategies` but Kong Gateway versions < 3.6.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D228
    Severity: error
    Description: |
      The plugin `konnect-application-auth` was defined with `auth_type` as
      `v2-strategies` but Kong Gateway versions < 3.6.0.0 do not support it. As a
      result, the entire configuration is rejected and Konnect will not update the
      configuration in the Data Plane Node until this defect is corrected.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D229
    Severity: error
    Description: |
      The `konnect-application-auth` plugin was configured with
      v2_strategies.openid_connect, where the configurations included changes that
      were added to the `openid-connect` plugin in Kong Gateway versions after
      3.6.0.0. For more details, please see the tracked changes for `openid-connect`.
    Resolution: |
      Please update Kong Gateway to the corresponding version where the fields were
      added.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D230
    Severity: error
    Description: |
      The `konnect-application-auth` plugin was configured with
      v2_strategies.key_auth, where the configurations included changes that were
      added to the `key-auth` plugin in Kong Gateway versions after 3.6.0.0. For more
      details, please see the tracked changes for `key-auth`.
    Resolution: |
      Please update Kong Gateway to the corresponding version where the fields were
      added.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D231
    Severity: error
    Description: |
      The `konnect-application-auth` plugin was configured with
      v2_strategies.openid_connect, where the configurations included changes that
      were added to the `openid-connect` plugin in Kong Gateway versions after
      3.6.0.0. For more details, please see the tracked changes for `openid-connect`.
    Resolution: |
      Please update Kong Gateway to the corresponding version where the fields were
      added.
    DocumentationURL: |
      [App Registration](/dev-portal/self-service/)
  - ID: D232
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, `config.identifier` field has been set
      to `consumer-group`. This is not supported in Kong Gateway versions < 3.6.0.0.
      The plugin configuration has been changed to `config.identifier=consumer` in the
      data plane node and might not work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D233
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more of the following fields:
      `tokens_count_strategy` have been set to one or more of the following values:
      `cost` but Kong Gateway versions < 3.8.0.0 do not support these values. Impacted
      fields have been updated to the following default values in the data plane node:
      `total_tokens`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D234
    Severity: error
    Description: |
      Plugin `ai-proxy-advanced` is not available in Kong Gateway versions < 3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D235
    Severity: error
    Description: |
      Plugin `ai-semantic-cache` is not available in Kong Gateway versions < 3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D236
    Severity: error
    Description: |
      Plugin `confluent` is not available in Kong Gateway versions < 3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D237
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `claims_forbidden` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D239
    Severity: error
    Description: |
      Plugin `header-cert-auth` is not available in Kong Gateway versions < 3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Header Cert Auth plugin](/plugins/header-cert-auth/)
  - ID: D240
    Severity: error
    Description: |
      Plugin `ai-semantic-prompt-guard` is not available in Kong Gateway versions <
      3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D241
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more instances have been
      configured with the `bedrock` LLM provider. This is not supported in Kong
      Gateway versions < 3.8.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D242
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more instances have been
      configured with the `gemini` LLM provider. This is not supported in Kong Gateway
      versions < 3.8.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D243
    Severity: error
    Description: |
      Plugin `json-threat-protection` is not available in Kong Gateway versions <
      3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [JSON Threat Protection plugin](/plugins/json-threat-protection/)
  - ID: D244
    Severity: error
    Description: |
      Plugin `upstream-oauth` is not available in Kong Gateway versions < 3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Upstream Oauth plugin](/plugins/upstream-oauth/)
  - ID: D245
    Severity: warning
    Description: |
      For the `rate-limiting-advanced` plugin, either `redis.sentinel_nodes` or
      `redis.cluster_nodes` are set. These settings are not supported in Kong Gateway
      versions < 3.8.0.0 and have been updated to `redis.sentinel_addresses` and
      `redis.cluster_addresses` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D246
    Severity: warning
    Description: |
      For the `rate-limiting-advanced` plugin, either `redis.sentinel_addresses` or
      `redis.cluster_addresses` are set. These settings are not supported in Kong
      Gateway versions >= 3.8.0.0 and have been updated to `redis.sentinel_nodes` and
      `redis.cluster_nodes` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D247
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `redis.cluster_max_redirections`, `redis.connection_is_proxied`
      but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D248
    Severity: error
    Description: |
      For the `proxy-cache-advanced` plugin, one or more of the following `config`
      fields are set: `redis.cluster_max_redirections`, `redis.connection_is_proxied`
      but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D249
    Severity: warning
    Description: |
      For the `proxy-cache-advanced` plugin, either `redis.sentinel_nodes` or
      `redis.cluster_nodes` are set. These settings are not supported in Kong Gateway
      versions < 3.8.0.0 and have been updated to `redis.sentinel_addresses` and
      `redis.cluster_addresses` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D250
    Severity: warning
    Description: |
      For the `proxy-cache-advanced` plugin, either `redis.sentinel_addresses` or
      `redis.cluster_addresses` are set. These settings are not supported in Kong
      Gateway versions >= 3.8.0.0 and have been updated to `redis.sentinel_nodes` and
      `redis.cluster_nodes` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D252
    Severity: warning
    Description: |
      For the `ai-rate-limiting-advanced` plugin, either `redis.sentinel_nodes` or
      `redis.cluster_nodes` are set. These settings are not supported in Kong Gateway
      versions < 3.8.0.0 and have been updated to `redis.sentinel_addresses` and
      `redis.cluster_addresses` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D253
    Severity: warning
    Description: |
      For the `ai-rate-limiting-advanced` plugin, either `redis.sentinel_addresses` or
      `redis.cluster_addresses` are set. These settings are not supported in Kong
      Gateway versions >= 3.8.0.0 and have been updated to `redis.sentinel_nodes` and
      `redis.cluster_nodes` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D254
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more of the following
      `config` fields are set: `redis.cluster_max_redirections`,
      `redis.connection_is_proxied` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D255
    Severity: error
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, one or more of the following
      `config` fields are set: `redis.cluster_max_redirections`,
      `redis.connection_is_proxied` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D256
    Severity: warning
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, either `redis.sentinel_nodes` or
      `redis.cluster_nodes` are set. These settings are not supported in Kong Gateway
      versions < 3.8.0.0 and have been updated to `redis.sentinel_addresses` and
      `redis.cluster_addresses` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D257
    Severity: warning
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, either `redis.sentinel_addresses`
      or `redis.cluster_addresses` are set. These settings are not supported in Kong
      Gateway versions >= 3.8.0.0 and have been updated to `redis.sentinel_nodes` and
      `redis.cluster_nodes` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D258
    Severity: error
    Description: |
      For the `saml` plugin, one or more of the following `config` fields are set:
      `redis` but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [SAML plugin](/plugins/saml/)
  - ID: D259
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `cluster_cache_strategy`, `cluster_cache_redis` but Kong Gateway
      versions < 3.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D260
    Severity: warning
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, either `redis.sentinel_nodes`
      or `redis.cluster_nodes` are set. These settings are not supported in Kong
      Gateway versions < 3.8.0.0 and have been updated to `redis.sentinel_addresses`
      and `redis.cluster_addresses` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D261
    Severity: warning
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, either
      `redis.sentinel_addresses` or `redis.cluster_addresses` are set. These settings
      are not supported in Kong Gateway versions >= 3.8.0.0 and have been updated to
      `redis.sentinel_nodes` and `redis.cluster_nodes` respectively.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D262
    Severity: error
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, one or more of the following
      `config` fields are set: `redis.cluster_max_redirections`,
      `redis.connection_is_proxied` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D263
    Severity: warning
    Description: |
      For the `rate-limiting-advanced` plugin, conflicting redis configurations are
      set. This is not supported in Kong Gateway versions < 3.8. The plugin
      configuration has been updated to remove the conflicting redis configurations.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D264
    Severity: warning
    Description: |
      For the `proxy-cache-advanced` plugin, conflicting redis configurations are set.
      This is not supported in Kong Gateway versions < 3.8. The plugin configuration
      has been updated to remove the conflicting redis configurations.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Proxy Cache Advanced plugin](/plugins/proxy-cache-advanced/)
  - ID: D265
    Severity: warning
    Description: |
      For the `ai-rate-limiting-advanced` plugin, conflicting redis configurations are
      set. This is not supported in Kong Gateway versions < 3.8. The plugin
      configuration has been updated to remove the conflicting redis configurations.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D266
    Severity: warning
    Description: |
      For the `graphql-proxy-cache-advanced` plugin, conflicting redis configurations
      are set. This is not supported in Kong Gateway versions < 3.8. The plugin
      configuration has been updated to remove the conflicting redis configurations.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Proxy Cache Advanced plugin](/plugins/graphql-proxy-cache-advanced/)
  - ID: D267
    Severity: warning
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, conflicting redis
      configurations are set. This is not supported in Kong Gateway versions < 3.8.
      The plugin configuration has been updated to remove the conflicting redis
      configurations.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D268
    Severity: warning
    Description: |
      For the `konnect-application-auth` plugin, one or more deprecated redis fields
      have been set on the plugin config. This is not supported in Kong Gateway
      versions >= 3.8. The plugin configuration has been updated.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D269
    Severity: warning
    Description: |
      For the `konnect-application-auth` plugin, new redis configurations are set.
      This is not supported in Kong Gateway versions <3.8. The plugin configuration
      has been updated.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D270
    Severity: warning
    Description: |
      For the `konnect-application-auth` and `openid-connect` plugins, new redis
      configurations are set. This is not supported in Kong Gateway versions <3.8. The
      plugin configuration has been updated.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D271
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `lock_dictionary_name` but Kong Gateway versions < 3.9.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D272
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set in a reference format: `http_proxy_authorization`,
      `https_proxy_authorization` but Kong Gateway versions < 3.9.0.0 do not support
      the reference format for these fields. This plugin instance will cause the Kong
      Gateway to fail payload validation, causing payload updates to fail and
      potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D273
    Severity: warning
    Description: |
      Plugin `redirect` is not available in Kong Gateway versions < 3.9.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Redirect plugin](/plugins/redirect/)
  - ID: D274
    Severity: error
    Description: |
      Plugin `service-protection` is not available in Kong Gateway versions < 3.9.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Service Protection plugin](/plugins/service-protection/)
  - ID: D275
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `compound_identifier` but Kong Gateway versions < 3.9.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D276
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more instances are configured with
      `targets[].model.provider` field set to: `huggingface` but Kong Gateway versions
      < 3.9.0.0 do not support this value. The impacted plugin has been removed from
      the configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D277
    Severity: error
    Description: |
      Plugin `injection-protection` is not available in Kong Gateway versions <
      3.9.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Injection Protection plugin](/plugins/injection-protection/)
  - ID: D278
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, one or more of the following `config` fields
      are set: `ignore_tool_prompts` but Kong Gateway versions < 3.9.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D279
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `response_streaming` but Kong Gateway versions < 3.9.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D280
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `redis.redis_proxy_type` but Kong Gateway versions < 3.9.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D281
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `introspection_post_args_client_headers` but Kong Gateway versions <
      3.9.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D282
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `targets[].model.options.huggingface` but Kong Gateway versions <
      3.9.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D283
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, instances have been configured on a specific
      consumer_group. This is not supported in Kong Gateway versions < 3.9.0.0. The
      plugin has been removed in the data plane node.
    Resolution: |
      Unset the `consumer_group` fields for the plugin, or upgrade Kong Gateway to a
      version >= 3.9.0.0.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D284
    Severity: warning
    Description: |
      A route expression is using deprecated fields
    Resolution: |
      Replace deprecated features with currently preferred ones
    DocumentationURL: |
      [Expressions Router](/gateway/routing/expressions)
  - ID: D285
    Severity: error
    Description: |
      For any `ai-proxy-advanced`, `ai-semantic-cache`, or `ai-semantic-prompt-guard`
      plugins, the `config.embeddings.model.name` field has been set to a free-form
      text value. This is not supported in Kong Gateway versions < 3.9.0.0. The plugin
      configuration has been updated to use the default embeddings model for the
      selected provider.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D286
    Severity: warning
    Description: |
      One or more of the following plugins are not supported for Cloud Gateways:
      `file-log`, `syslog`, `app-dynamics`, `prometheus`.
    Resolution: |
      Remove the unsupported plugins
    DocumentationURL: |
      [Kong Gateway Plugins Compatibility](/plugins/compatibility/)
  - ID: D290
    Severity: error
    Description: |
      For the `json-threat-protection` plugin, one or more of the following `config`
      fields are set: `allow_duplicate_object_entry_name` but Kong Gateway versions <
      3.10.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [JSON Threat Protection plugin](/plugins/json-threat-protection/)
  - ID: D291
    Severity: warning
    Description: |
      For AWS vaults, the `config.region` field has been set to an unsupported value
      in Kong Gateway versions <3.4.3.17 for the 3.4.x series, <3.7.1.5 for the 3.7.x
      series, <3.8.1.1 for the 3.8.x series, or <3.9.1.0 for the 3.9.x series. The
      vault has been removed from the configuration.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.17` or above for the 3.4.x series,
      `3.7.1.5` or above for the 3.7.x series, 3.8.1.1 or above for the 3.8.x series,
      3.9.1.0 or above for the 3.9.x series or newer than `3.10.0.0`, or use a
      supported AWS region.
    DocumentationURL: |
      [Vault provider-specific configuration parameters](/gateway/entities/vault/?tab=aws#vault-provider-specific-configuration-parameters)
  - ID: D292
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `allowed_topics` but Kong Gateway versions < 3.10.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D293
    Severity: error
    Description: |
      Plugin `ai-rag-injector` is not available in Kong Gateway versions < 3.10.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI RAG Injector plugin](/plugins/ai-rag-injector/)
  - ID: D294
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `allowed_topics` but Kong Gateway versions < 3.10.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D295
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `message_by_lua_functions` but Kong Gateway versions < 3.10.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D296
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `message_by_lua_functions` but Kong Gateway versions < 3.10.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D297
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `topics_query_arg` but Kong Gateway versions < 3.10.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D298
    Severity: error
    Description: |
      Plugin `ai-sanitizer` is not available in Kong Gateway versions < 3.10.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Sanitizer plugin](/plugins/ai-sanitizer/)
  - ID: D299
    Severity: error
    Description: |
      Plugin `request-callout` is not available in Kong Gateway versions < 3.10.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Request Callout plugin](/plugins/request-callout/)
  - ID: D300
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, `config.balancer.algorithm` has been set to
      `priority`. This is not supported in Kong Gateway versions < 3.10.0.0. The
      plugin configuration has been changed to `config.balancer.algorithm=round-robin`
      in the data plane node (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D301
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `topics_query_arg` but Kong Gateway versions < 3.10.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D302
    Severity: error
    Description: |
      Plugin `kafka-consume` is not available in Kong Gateway versions < 3.10.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D303
    Severity: error
    Description: |
      Plugin `confluent-consume` is not available in Kong Gateway versions < 3.10.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D304
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, `config.balancer.tokens_count_strategy` has
      been set to `cost`. This is not supported in Kong Gateway versions < 3.10.0.0.
      The plugin configuration has been changed to
      `config.balancer.tokens_count_strategy=total-tokens` in the data plane node
      (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D305
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `balancer.failover_criteria` but Kong Gateway versions < 3.10.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D307
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more instances are configured
      with llm.model.provider `huggingface`.This is not supported in Kong Gateway
      versions < 3.9.0.0.The impacted plugin have been removed from the configuration.
      Because of this, features that rely on these plugins are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: D308
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.huggingface` but Kong Gateway versions <
      3.9.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: D309
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more instances are configured
      with llm.model.provider `huggingface`.This is not supported in Kong Gateway
      versions < 3.9.0.0.The impacted plugin have been removed from the configuration.
      Because of this, features that rely on these plugins are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: D310
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, the following field:type pairs were
      detected: `window_size:array`, `limit:array`. Field(s) in Kong Gateway versions
      < 3.10.0.0 use the following type(s): `window_size:number`, `limit:number`. The
      following action(s) have been taken: impacted fields converted to numeric type,
      impacted fields set to the first element in the array. Features that rely on
      these fields may not be working as intended.
    Resolution: |
      Please take one or more of the following actions: Please upgrade Kong Gateway to
      version `3.10.0.0` or above., Update the plugin configuration to use the correct
      field types.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D311
    Severity: warning
    Description: |
      For the `ai-rate-limiting-advanced` plugin, the following field:type pairs were
      detected: `window_size:number`, `limit:number`. Field(s) in Kong Gateway
      versions >= 3.10.0.0 use the following type(s): `window_size:array`,
      `limit:array`. The following action(s) have been taken: numbers were converted
      to arrays on impacted fields. Features that rely on these fields may not be
      working as intended.
    Resolution: |
      Please take one or more of the following actions: Update the plugin
      configuration to use the new field types
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D312
    Severity: error
    Description: |
      For the `graphql-rate-limiting-advanced` plugin, one or more of the following
      `config` fields are set: `pass_all_downstream_headers` but Kong Gateway versions
      < 3.11.0.0 do not support these fields. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: D321
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `embeddings.model.options.huggingface`,
      `embeddings.model.options.azure`, `embeddings.model.options.bedrock`,
      `embeddings.model.options.gemini` but Kong Gateway versions < 3.10.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D322
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, `config.embeddings.model.provider` has been
      set to `huggingface`, `azure`, `bedrock` or `gemini`. This is not supported in
      Kong Gateway versions < 3.10.0.0. The plugin configuration has been changed to
      `config.embeddings.model.provider=openai` in the data plane node (default
      value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D323
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, one or more of the following `config` fields
      are set: `embeddings.model.options.huggingface`,
      `embeddings.model.options.azure`, `embeddings.model.options.bedrock`,
      `embeddings.model.options.gemini` but Kong Gateway versions < 3.10.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D324
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, `config.embeddings.model.provider` has been
      set to `huggingface`, `azure`, `bedrock` or `gemini`. This is not supported in
      Kong Gateway versions < 3.10.0.0. The plugin configuration has been changed to
      `config.embeddings.model.provider=openai` in the data plane node (default
      value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D325
    Severity: error
    Description: |
      For the `ai-semantic-prompt-guard` plugin, one or more of the following `config`
      fields are set: `embeddings.model.options.huggingface`,
      `embeddings.model.options.azure`, `embeddings.model.options.bedrock`,
      `embeddings.model.options.gemini` but Kong Gateway versions < 3.10.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D326
    Severity: error
    Description: |
      For the `ai-semantic-prompt-guard` plugin, `config.embeddings.model.provider`
      has been set to `huggingface`, `azure`, `bedrock` or `gemini`. This is not
      supported in Kong Gateway versions < 3.10.0.0. The plugin configuration has been
      changed to `config.embeddings.model.provider=openai` in the data plane node
      (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D327
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, one or more of the following `config` fields
      are set: `vectordb.pgvector` but Kong Gateway versions < 3.10.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D328
    Severity: error
    Description: |
      For the `ai-semantic-prompt-guard` plugin, one or more of the following `config`
      fields are set: `vectordb.pgvector` but Kong Gateway versions < 3.10.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D329
    Severity: error
    Description: |
      For any `ai-proxy-advanced`, `ai-semantic-cache`, `ai-semantic-prompt-guard`,
      `ai-proxy`, `ai-prompt-decorator`, `ai-prompt-guard` or
      `ai-rate-limiting-advanced` plugins, the `config.llm_format` field has been
      configured. This is not supported in Kong Gateway versions < 3.10.0.0. The field
      has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Kong Plugin Hub](/plugins/)
  - ID: D330
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `vectordb.pgvector` but Kong Gateway versions < 3.10.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D331
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more instances have been
      configured with the `huggingface` LLM provider. This is not supported in Kong
      Gateway versions < 3.9.0.0. The llm provider has been replaced by
      `requestPrompt` in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D332
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `targets[].model.options.bedrock.aws_role_session_name`,
      `targets[].model.options.bedrock.aws_sts_endpoint_url`,
      `targets[].model.options.bedrock.aws_assume_role_arn` but Kong Gateway versions
      < 3.10.0.0 do not support these fields. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D333
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, the `config.vectordb.strategy` field has
      been configured to `pgvector`. This is not supported in Kong Gateway versions <
      3.10.0.0. The field has been migrated to `redis`.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D334
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, the `config.vectordb.strategy` field has
      been configured to `pgvector`. This is not supported in Kong Gateway versions <
      3.10.0.0. The field has been migrated to `redis`.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D335
    Severity: error
    Description: |
      For the `ai-semantic-prompt-guard` plugin, the `config.vectordb.strategy` field
      has been configured to `pgvector`. This is not supported in Kong Gateway
      versions < 3.10.0.0. The field has been migrated to `redis`.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D336
    Severity: error
    Description: |
      For the plugins including `ai-proxy-advanced`, `graphql-proxy-cache-advanced`,
      `graphql-rate-limiting-advanced`, `openid-connect`, `proxy-cache-advanced`,
      `rate-limiting-advanced`, `request-callout`, `saml`, `service-protection`, and
      `upstream-oauth`, the redis timeout fields `connect_timeout`, `read_timeout`,
      and `send_timeout` have conflicting values. This is not supported in Kong
      Gateway versions >= 3.8.0.0 and < 3.10.0.1. The affected plugins have been
      removed from the configuration.
    Resolution: |
      Please either update the redis timeout values to be the same or upgrade to Kong
      Gateway to versions 3.10.0.1 and above.
    DocumentationURL: |
      [Kong Plugins](/plugins/)
  - ID: D337
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `key_query_arg` but Kong Gateway versions < 3.11.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D338
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `key_query_arg` but Kong Gateway versions < 3.11.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D339
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `key_query_arg` but Kong Gateway versions < 3.11.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D340
    Severity: error
    Description: |
      Plugin `ai-prompt-compressor` is not available in Kong Gateway versions <
      3.11.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Prompt Compressor plugin](/plugins/ai-prompt-compressor/)
  - ID: D341
    Severity: error
    Description: |
      For the `vault` entity, one or more of the following schema fields are set:
      `base64_decode` but Kong Gateway versions < 3.11.0.0 do not support these
      fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Vault entity](/gateway/entities/vault/)
  - ID: D342
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more of the following
      `config` fields are set: `namespace` but Kong Gateway versions < 3.11.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D343
    Severity: error
    Description: |
      Plugin `ai-llm-as-judge` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI LLM as Judge plugin](/plugins/ai-llm-as-judge/)
  - ID: D349
    Severity: error
    Description: |
      The `vault(conjur)` entity is being used, but Kong Gateway versions < 3.11.0.0
      do not support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Vault entity](/gateway/entities/vault/#vault-provider-specific-configuration-parameters)
  - ID: D350
    Severity: error
    Description: |
      For the `vaults` entity, one or more of the following schema fields are set:
      `auth_method=cert` but Kong Gateway versions < 3.11.0.0 do not support these
      fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Vault entity](/gateway/entities/vault/)
  - ID: D351
    Severity: error
    Description: |
      For the `vaults` entity, one or more of the following schema fields are set:
      `cert_auth_role_name`, `cert_auth_cert`, `cert_auth_cert_key` but Kong Gateway
      versions < 3.11.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Vault entity](/gateway/entities/vault/)
  - ID: D352
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more instances are configured with `mode`
      field set to: `websocket` but Kong Gateway versions < 3.11.0.0 do not support
      this value. The impacted plugin has been removed from the configuration. Because
      of this, features that rely on these plugins are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D353
    Severity: error
    Description: |
      For the `upstream` entity, one or more instances are configured with either the
      algorithm field set to `sticky-sessions`, or the fields `sticky_sessions_cookie`
      and `sticky_sessions_cookie_path` set to non-default values. However, Kong
      Gateway versions earlier than `3.11.0.0` do not support these configurations.
      The affected upstreams have been updated with the `algorithm` value of
      `consistent-hashing`, and the fields `sticky_sessions_cookie` and
      `sticky_sessions_cookie_path` have been removed. As a result, features that rely
      on these plugins are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Upstream entity](/gateway/entities/upstream/)
  - ID: D354
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, the `protocol` field has been set to one of:
      `ws`, `wss`. This is not supported in Kong Gateway versions < 3.11.0.0.
      Unsupported protocols have been removed. If no protocols are left, the plugin
      has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D355
    Severity: error
    Description: |
      Plugin `solace-upstream` is not available in Kong Gateway versions < 3.11.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Solace Upstream plugin](/plugins/solace-upstream/)
  - ID: D356
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `schema_registry` but Kong Gateway versions < 3.11.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D357
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `schema_registry` but Kong Gateway versions < 3.11.0.0 do not support
      these fields and have been removed. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D358
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `schema_registry` but Kong Gateway versions < 3.11.0.0 do not support these
      fields and have been removed. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D359
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `schema_registry` but Kong Gateway versions < 3.11.0.0 do not support these
      fields and have been removed. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D360
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `schema_registry` but Kong Gateway versions < 3.11.0.0 do not support
      these fields and have been removed. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D361
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `topics[].schema_registry` but Kong Gateway versions < 3.11.0.0 do not
      support these fields and have been removed. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D362
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `topics[].schema_registry` but Kong Gateway versions < 3.11.0.0 do not
      support these fields and have been removed. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D364
    Severity: error
    Description: |
      For the `service` entity, one or more of the following schema fields are set:
      `service.tls_sans` but Kong Gateway versions < 3.10.0.6 do not support these
      fields.The unsupported fields have been removed from the entity configuration.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.6` or above.
    DocumentationURL: |
      [Service entity](/gateway/entities/service/)
  - ID: D365
    Severity: error
    Description: |
      Plugin `ai-aws-guardrails` is not available in Kong Gateway versions < 3.11.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI AWS Guardrails plugin](/plugins/ai-aws-guardrails/)
  - ID: D367
    Severity: error
    Description: |
      Plugin `ai-mcp-proxy` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: D368
    Severity: error
    Description: |
      Plugin `datakit` is not available in Kong Gateway versions < 3.11.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Datakit plugin](/plugins/datakit/)
  - ID: D369
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more instance targets are configured
      with `llm_format` set to `bedrock/gemini` and a `route_type` set to `preserve`
      but Kong Gateway versions < 3.11.0.0 do not support this value. The impacted
      targets have been removed from the configuration. Because of this, features that
      rely on these plugins are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D370
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more instance targets are configured
      with `route_type` field set to one of: `llm/v1/assistants`,
      `audio/v1/audio/speech`, `audio/v1/audio/transcriptions`,
      `audio/v1/audio/translations`, `llm/v1/batches`, `llm/v1/embeddings`,
      `llm/v1/files`, `image/v1/images/edits`, `image/v1/images/generations`,
      `realtime/v1/realtime`, `llm/v1/responses` but Kong Gateway versions < 3.11.0.0
      do not support this value. The impacted targets have been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D371
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `targets.#.model.options.embeddings_dimensions` but Kong Gateway
      versions < 3.11.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D372
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `targets[].model.options.cohere` but Kong Gateway versions < 3.11.0.0
      do not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D390
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `embeddings.model.options.bedrock.embeddings_normalize` but Kong
      Gateway versions < 3.11.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D391
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `targets.#.model.options.bedrock.embeddings_normalize` but Kong Gateway
      versions < 3.11.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D392
    Severity: error
    Description: |
      For the `ai-rag-injector` plugin, one or more of the following `config` fields
      are set: `embeddings.model.options.bedrock.embeddings_normalize` but Kong
      Gateway versions < 3.11.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI RAG Injector plugin](/plugins/ai-rag-injector/)
  - ID: D393
    Severity: error
    Description: |
      For the `ai-semantic-cache` plugin, one or more of the following `config` fields
      are set: `embeddings.model.options.bedrock.embeddings_normalize` but Kong
      Gateway versions < 3.11.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D394
    Severity: error
    Description: |
      For the `ai-semantic-prompt-guard` plugin, one or more of the following `config`
      fields are set: `embeddings.model.options.bedrock.embeddings_normalize` but Kong
      Gateway versions < 3.11.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D395
    Severity: warning
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `cohere`,
      `huggingface` but Kong Gateway versions < 3.11.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `openai`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D396
    Severity: warning
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `cohere`,
      `huggingface` but Kong Gateway versions < 3.11.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `openai`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D397
    Severity: warning
    Description: |
      For the `ai-semantic-cache` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `cohere`,
      `huggingface` but Kong Gateway versions < 3.11.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `openai`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Semantic Cache plugin](/plugins/ai-semantic-cache/)
  - ID: D398
    Severity: warning
    Description: |
      For the `ai-semantic-prompt-guard` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `cohere`,
      `huggingface` but Kong Gateway versions < 3.11.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `openai`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D399
    Severity: error
    Description: |
      For the `ai-semantic-prompt-guard` plugin, one or more of the following `config`
      fields are set: `genai_category` but Kong Gateway versions < 3.11.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Semantic Prompt Guard plugin](/plugins/ai-semantic-prompt-guard/)
  - ID: D400
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `genai_category` but Kong Gateway versions < 3.11.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D401
    Severity: error
    Description: |
      For the `route` entity, one or more of the following schema fields are set:
      `path_handling` but Kong Gateway versions < 3.7.0.0 do not support these
      fields.The unsupported fields have been removed from the entity configuration.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Route entity](/gateway/entities/route/)
  - ID: D402
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `message_by_lua_functions` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D403
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `message_by_lua_functions` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D405
    Severity: error
    Description: |
      Plugin `ai-semantic-response-guard` is not available in Kong Gateway versions <
      3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Semantic Response Guard plugin](/plugins/ai-semantic-response-guard/)
  - ID: D406
    Severity: error
    Description: |
      For the `ai-azure-content-safety` plugin, one or more of the following `config`
      fields are set: `guarding_mode` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Azure Content Safety plugin](/plugins/ai-azure-content-safety/)
  - ID: D407
    Severity: error
    Description: |
      For the `ai-azure-content-safety` plugin, one or more of the following `config`
      fields are set: `stop_on_error` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Azure Content Safety plugin](/plugins/ai-azure-content-safety/)
  - ID: D408
    Severity: error
    Description: |
      For the `ai-azure-content-safety` plugin, one or more of the following `config`
      fields are set: `response_buffer_size` but Kong Gateway versions < 3.12.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Azure Content Safety plugin](/plugins/ai-azure-content-safety/)
  - ID: D409
    Severity: error
    Description: |
      For the `ai-aws-guardrails` plugin, one or more of the following `config` fields
      are set: `aws_assume_role_arn`, `aws_role_session_name`, `aws_sts_endpoint_url`
      but Kong Gateway versions < 3.12.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI AWS Guardrails plugin](/plugins/ai-aws-guardrails/)
  - ID: D410
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following `config`
      fields are set: `throttling` but Kong Gateway versions < 3.12.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D411
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following fields:
      `balancer.tokens_count_strategy` have been set to one or more of the following
      values: `llm-accuracy` but Kong Gateway versions < 3.12.0.0 do not support these
      values. Impacted fields have been updated to the following default values in the
      data plane node: `llm-accuracy`. Plugin features that rely on these values may
      not be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D412
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `schema_registry.confluent.authentication.oauth2`,
      `schema_registry.confluent.authentication.oauth2_client` but Kong Gateway
      versions < 3.12.0.0 do not support these fields and have been removed. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D413
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `topics[].schema_registry.confluent.authentication.oauth2`,
      `topics[].schema_registry.confluent.authentication.oauth2_client` but Kong
      Gateway versions < 3.12.0.0 do not support these fields and have been removed.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D414
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `schema_registry.confluent.authentication.oauth2`,
      `schema_registry.confluent.authentication.oauth2_client` but Kong Gateway
      versions < 3.12.0.0 do not support these fields and have been removed. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D415
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `topics[].schema_registry.confluent.authentication.oauth2`,
      `topics[].schema_registry.confluent.authentication.oauth2_client` but Kong
      Gateway versions < 3.12.0.0 do not support these fields and have been removed.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D416
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `schema_registry.confluent.authentication.oauth2`,
      `schema_registry.confluent.authentication.oauth2_client` but Kong Gateway
      versions < 3.12.0.0 do not support these fields and have been removed. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D417
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `schema_registry.confluent.authentication.oauth2`,
      `schema_registry.confluent.authentication.oauth2_client` but Kong Gateway
      versions < 3.12.0.0 do not support these fields and have been removed. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D418
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `schema_registry.confluent.authentication.oauth2`,
      `schema_registry.confluent.authentication.oauth2_client` but Kong Gateway
      versions < 3.12.0.0 do not support these fields and have been removed. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D420
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `enable_dlq`, `dlq_topic` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D421
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `enable_dlq`, `dlq_topic` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D422
    Severity: error
    Description: |
      For the `confluent-consume` plugin, the `protocol` field has been set to one of:
      `ws`, `wss`. This is not supported in Kong Gateway versions < 3.12.0.0.
      Unsupported protocols have been removed. If no protocols are left, the plugin
      has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D423
    Severity: error
    Description: |
      For the `ai-sanitizer` plugin, one or more of the following `config` fields are
      set: `sanitization_mode`, `block_if_detected` but Kong Gateway versions <
      3.12.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Sanitizer plugin](/plugins/ai-sanitizer/)
  - ID: D424
    Severity: error
    Description: |
      Plugin `solace-consume` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Solace Consume plugin](/plugins/solace-consume/)
  - ID: D425
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `targets[].model.options.gemini.endpoint_id` but Kong Gateway versions
      < 3.12.0.0 do not support these fields. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D426
    Severity: error
    Description: |
      For the `kafka-consume` plugin, instances have been configured on a specific
      service. This is not supported in Kong Gateway versions < 3.2.0.0. The plugin
      has been removed in the data plane node.
    Resolution: |
      Unset the `service` fields for the plugin
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D427
    Severity: error
    Description: |
      Plugin `ace` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D428
    Severity: error
    Description: |
      Konnect cloud's ACE plugin workflow requires features introduced in Kong Gateway
      3.12.0.0 and above. Some features of the ACE are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D429
    Severity: error
    Description: |
      Plugin `ai-gcp-model-armor` is not available in Kong Gateway versions <
      3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI GCP Model Armor plugin](/plugins/ai-gcp-model-armor/)
  - ID: D430
    Severity: error
    Description: |
      For the `request-callout` plugin, the following field:type pairs were detected:
      `request.url:string`. Field(s) in Kong Gateway versions < 3.12.0.0 use the
      following type(s): `request.url:url`. The following action(s) have been taken:
      plugin removed. Features that rely on these fields may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Request Callout plugin](/plugins/request-callout/)
  - ID: D431
    Severity: error
    Description: |
      Plugin `solace-log` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Solace Log plugin](/plugins/solace-log/)
  - ID: D432
    Severity: error
    Description: |
      For the `injection-protection` plugin, one or more of the following fields:
      `locations` have been set to one or more of the following values: `path`,
      `query` but Kong Gateway versions < 3.12.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `path_and_query`. Plugin features that rely on these values may not
      be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Injection Protection plugin](/plugins/injection-protection/)
  - ID: D433
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `consumer_groups_claim`, `consumer_groups_optional` but Kong Gateway
      versions < 3.12.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D434
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `bind` but Kong Gateway versions < 3.12.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: D435
    Severity: error
    Description: |
      Plugin `jwt-signer` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [JWT Signer plugin](/plugins/jwt-signer/)
  - ID: D436
    Severity: error
    Description: |
      Plugin `ai-mcp-oauth2` is not available in Kong Gateway versions < 3.12.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI MCP OAuth2 plugin](/plugins/ai-mcp-oauth2/)
  - ID: D437
    Severity: error
    Description: |
      For the `solace-upstream` plugin, one or more of the following `config` fields
      are set: `session.calculate_message_expiry`, `session.generate_rcv_timestamps`,
      `session.generate_send_timestamps`, `session.generate_sender_id`,
      `session.generate_sequence_number` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [Solace Upstream plugin](/plugins/solace-upstream/)
  - ID: D438
    Severity: error
    Description: |
      Unsupported Datakit node type
    Resolution: |
      Remove the invalid Datakit node or upgrade to a version that supports it.
    DocumentationURL: |
      [Plugins Datakit](/plugins/datakit/)
  - ID: D439
    Severity: error
    Description: |
      Unsupported Datakit config element
    Resolution: |
      Remove the invalid Datakit config element or upgrade to a version that supports
      it.
    DocumentationURL: |
      [Plugins Datakit](/plugins/datakit/)
  - ID: D440
    Severity: error
    Description: |
      For the `targets` entity, one or more of the following schema fields are set:
      `failover` but Kong Gateway versions < 3.12.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [Targets](/gateway/entities/target/)
  - ID: D441
    Severity: error
    Description: |
      For the `solace-upstream` plugin, one or more of the following `config` fields
      are set: `session.properties.SESSION_GUARANTEED_WITH_WEB_TRANSPORT` but Kong
      Gateway versions >= 3.12.0.0 do not support these fields and have been removed.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please take one or more of the following actions: Remove the field
      `SESSION_GUARANTEED_WITH_WEB_TRANSPORT` from `session.properties` in the plugin
      configuration
    DocumentationURL: |
      [Solace Upstream plugin](/plugins/solace-upstream/)
  - ID: D442
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `enforce_latest_offset_reset` but Kong Gateway versions < 3.10.0.6 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please set the `enforce_latest_offset_reset` field to false.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D443
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `enforce_latest_offset_reset` but Kong Gateway versions < 3.10.0.6 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please set the `enforce_latest_offset_reset` field to false.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D444
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more instances have been
      configured with the `decrease_by_fractions_in_redis` field set to `true`. This
      field is not supported in Kong Gateway versions < 3.10.0.7, >= 3.11.0.0 and <
      3.11.0.5, or >= 3.12.0.0 and < 3.12.0.1. The field has been removed in the data
      plane node.
    Resolution: |
      Please set the `decrease_by_fractions_in_redis` field to false.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: D445
    Severity: error
    Description: |
      For the `vaults` entity, one or more of the following schema fields are set:
      `auth_method=jwt` but Kong Gateway versions < 3.13.0.0 do not support these
      fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Vault entity](/gateway/entities/vault/)
  - ID: D446
    Severity: error
    Description: |
      For the `vaults` entity, one or more of the following schema fields are set:
      `jwt_role`, `oauth2_token_endpoint`, `oauth2_client_id`, `oauth2_client_secret`,
      `oauth2_audiences` but Kong Gateway versions < 3.13.0.0 do not support these
      fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Vault entity](/gateway/entities/vault/)
  - ID: D447
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more of the following fields:
      `identifier` have been set to one or more of the following values: `route` but
      Kong Gateway versions < 3.13.0.0 do not support these values. Impacted fields
      have been updated to the following default values in the data plane node: `ip`.
      Plugin features that rely on these values may not be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D448
    Severity: error
    Description: |
      For plugins that supports Redis,the `cloud_authentication` field has been set.
      This is not supported in Kong Gateway versions < 3.13.0.0. The
      `cloud_authentication` field have been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: D449
    Severity: error
    Description: |
      For the `rate-limiting-advanced` plugin, one or more instances are configured
      with `compound_identifier` field set to: `route` but Kong Gateway versions <
      3.13.0.0 do not support this value. The impacted plugin has been removed from
      the configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: D450
    Severity: error
    Description: |
      For the `ai-aws-guardrails` plugin, one or more of the following `config` fields
      are set: `allow_masking` but Kong Gateway versions < 3.13.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI AWS Guardrails plugin](/plugins/ai-aws-guardrails/)
  - ID: D451
    Severity: error
    Description: |
      Plugin `ai-lakera-guard` is not available in Kong Gateway versions < 3.13.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Lakera Guard plugin](/plugins/ai-lakera-guard/)
  - ID: D452
    Severity: error
    Description: |
      For the `ai-proxy` plugin, the provider `xAI` is only compatible with Kong
      versions 3.13.0.0 and above. The provider name has been set to `OpenAI` which
      provides best-effort compatibility with xAI and the Grok AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: D453
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, the provider `xAI` is only compatible
      with Kong versions 3.13.0.0 and above. The provider name has been set to
      `OpenAI` which provides best-effort compatibility with xAI and the Grok AI
      models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: D454
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, the provider `xAI` is only compatible
      with Kong versions 3.13.0.0 and above. The provider name has been set to
      `OpenAI` which provides best-effort compatibility with xAI and the Grok AI
      models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: D455
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more instance targets are configured
      with `model.provider` field set to `xai` but Kong Gateway versions < 3.13.0.0 do
      not support this value. The impacted targets have been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D459
    Severity: error
    Description: |
      For the `ai-proxy` plugin, the provider `Cerebras` is only compatible with Kong
      versions 3.13.0.0 and above. The provider name has been set to `OpenAI` which
      provides best-effort compatibility with Cerebras AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: D460
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, the provider `Cerebras` is only
      compatible with Kong versions 3.13.0.0 and above. The provider name has been set
      to `OpenAI` which provides best-effort compatibility with Cerebras AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: D461
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `balancer.max_fails`, `balancer.fail_timeout` but Kong Gateway versions
      < 3.13.0.0 do not support these fields. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D462
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, the provider `Cerebras` is only
      compatible with Kong versions 3.13.0.0 and above. The provider name has been set
      to `OpenAI` which provides best-effort compatibility with Cerebras AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: D463
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more instance targets are configured
      with `model.provider` field set to `cerebras` but Kong Gateway versions <
      3.13.0.0 do not support this value. The impacted targets have been removed from
      the configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D464
    Severity: error
    Description: |
      For the `ai-sanitizer` plugin, one or more of the following `config` fields are
      set: `skip_logging_sanitized_items` but Kong Gateway versions < 3.13.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Sanitizer plugin](/plugins/ai-sanitizer/)
  - ID: D465
    Severity: error
    Description: |
      For the `ai-rag-injector` plugin, one or more of the following `config` fields
      are set: `global_acl_config`, `collection_acl_config`, `max_filter_clauses`,
      `filter_mode`, `stop_on_filter_error`, `consumer_identifier` but Kong Gateway
      versions < 3.13.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI RAG Injector plugin](/plugins/ai-rag-injector/)
  - ID: D469
    Severity: error
    Description: |
      For the `ai-mcp-proxy` plugin, one or more tools have an input scheme where one
      or more of the parameters have `in` set to `cookie`, but Kong Gateway versions <
      3.13.0.0 do not support this value. The impacted parameters have been removed
      from these tools. Because of this, features that rely on these parameters will
      not work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: D470
    Severity: error
    Description: |
      For the `ai-mcp-proxy` plugin, one or more tools have a structured output
      `response` schema defined, but Kong Gateway versions < 3.13.0.0 do not support
      this. The response schema has been removed from these tools. Because of this,
      features that rely on these parameters will not work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: D472
    Severity: error
    Description: |
      For the `ai-proxy` plugin, the provider `Dashscope` is only compatible with Kong
      versions 3.13.0.0 and above. The provider name has been set to `OpenAI` which
      provides best-effort compatibility with Dashscope AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: D473
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, the provider `Dashscope` is only
      compatible with Kong versions 3.13.0.0 and above. The provider name has been set
      to `OpenAI` which provides best-effort compatibility with Dashscope AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: D474
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, the provider `Dashscope` is only
      compatible with Kong versions 3.13.0.0 and above. The provider name has been set
      to `OpenAI` which provides best-effort compatibility with Dashscope AI models.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: D475
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more instance targets are configured
      with `model.provider` field set to `dashscope` but Kong Gateway versions <
      3.13.0.0 do not support this value. The impacted targets have been removed from
      the configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D477
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following `config` fields
      are set: `target[].metadata` but Kong Gateway versions < 3.13.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: D478
    Severity: error
    Description: |
      For the `ai-llm-as-judge` plugin, one or more of the following `config` fields
      are set: `llm.weight`, `llm.metadata`, `llm.description` but Kong Gateway
      versions < 3.13.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI LLM as Judge plugin](/plugins/ai-llm-as-judge/)
  - ID: D479
    Severity: error
    Description: |
      For the `ai-rag-injector` plugin, one or more of the following `config` fields
      are set: `vectordb.threshold` but Kong Gateway versions < 3.13.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI RAG Injector plugin](/plugins/ai-rag-injector/)
  - ID: D480
    Severity: error
    Description: |
      Unsupported field `ssl_verify` for datakit call nodes
    Resolution: |
      Remove the invalid field or upgrade the Kong Gateway to version `3.13.0.0` or
      above.
    DocumentationURL: |
      [Plugins Datakit](/plugins/datakit/)
  - ID: D481
    Severity: error
    Description: |
      For the `mtls-auth` plugin, one or more of the following `config` fields are
      set: `ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [mTLS Authentication plugin](/plugins/mtls-auth/)
  - ID: D482
    Severity: error
    Description: |
      For the `ai-aws-guardrails` plugin, one or more of the following `config` fields
      are set: `ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI AWS Guardrails plugin](/plugins/ai-aws-guardrails/)
  - ID: D483
    Severity: error
    Description: |
      For the `jwt-signer` plugin, one or more of the following `config` fields are
      set: `access_token_endpoints_ssl_verify`, `channel_token_endpoints_ssl_verify`
      but Kong Gateway versions < 3.13.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [JWT Signer plugin](/plugins/jwt-signer/)
  - ID: D484
    Severity: error
    Description: |
      For the `ai-azure-content-safety` plugin, one or more of the following `config`
      fields are set: `ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Azure Content Safety plugin](/plugins/ai-azure-content-safety/)
  - ID: D485
    Severity: error
    Description: |
      For the `header-cert-auth` plugin, one or more of the following `config` fields
      are set: `ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Header Certificate Authentication plugin](/plugins/header-cert-auth/)
  - ID: D486
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `session_memcached_ssl`, `session_memcached_ssl_verify` but Kong
      Gateway versions < 3.13.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: D487
    Severity: error
    Description: |
      For the `kafka-log` plugin, one or more of the following `config` fields are
      set: `security.ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Kafka Log plugin](/plugins/kafka-log/)
  - ID: D488
    Severity: error
    Description: |
      For the `kafka-consume` plugin, one or more of the following `config` fields are
      set: `security.ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Kafka Consume plugin](/plugins/kafka-consume/)
  - ID: D489
    Severity: error
    Description: |
      For the `kafka-upstream` plugin, one or more of the following `config` fields
      are set: `security.ssl_verify` but Kong Gateway versions < 3.13.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Kafka Upstream plugin](/plugins/kafka-upstream/)
  - ID: D490
    Severity: error
    Description: |
      Bedrock provider does not support auth.allow_override=true in Gateway versions
      before 3.13.0.0. This configuration uses Bedrock with auth.allow_override=true
      which requires Gateway 3.13.0.0 or above. The value has been updated to false in
      order to preserve compatibility with older dataplane versions. The AI Proxy
      Plugin might not work as expected as a result.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: D491
    Severity: error
    Description: |
      For the `confluent-consume` plugin, one or more of the following `config` fields
      are set: `security` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Confluent Consume plugin](/plugins/confluent-consume/)
  - ID: D492
    Severity: error
    Description: |
      For the `confluent` plugin, one or more of the following `config` fields are
      set: `security` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Confluent plugin](/plugins/confluent/)
  - ID: D497
    Severity: error
    Description: |
      For the `oauth2-introspection` plugin, the `authorization_value` config was
      found to contain a value matching the referenceable field format, but only Kong
      Gateway versions >= 3.6.0.0 support this field. Plugin features that rely on
      this field are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [OAuth 2.0 Introspection plugin](/plugins/oauth2-introspection/)
  - ID: EOL100
    Severity: error
    Description: |
      Kong Gateway version 2.5.x.x has now reached its End of Sunset Support, and
      after July 31, 2024, data plane connections for this version will no longer
      receive configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 2.8 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL101
    Severity: error
    Description: |
      Kong Gateway version 2.6.x.x has now reached its End of Full Support, and after
      July 31, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 2.8 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL102
    Severity: error
    Description: |
      Kong Gateway version 2.7.x.x has now reached its End of Full Support, and after
      July 31, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 2.8 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL103
    Severity: error
    Description: |
      Kong Gateway version 3.0.x.x has now reached its End of Full Support, and after
      July 31, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 3.4 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL104
    Severity: error
    Description: |
      Kong Gateway version 3.1.x.x has now reached its End of Full Support, and after
      July 31, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 3.4 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL105
    Severity: error
    Description: |
      Kong Gateway version 3.2.x.x has now reached its End of Full Support, and after
      July 31, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 3.4 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL106
    Severity: error
    Description: |
      Kong Gateway version 3.3.x.x has now reached its End of Full Support, and after
      July 31, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 3.4 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: EOL107
    Severity: error
    Description: |
      Kong Gateway version 3.5.x.x has now reached its End of Full Support, and after
      Dec 1, 2024, data plane connections for this version will no longer receive
      configurations from Konnect.
    Resolution: |
      To ensure continued Konnect support, please upgrade to the latest 3.10 LTS
      version.
    DocumentationURL: |
      [Compatibility](/konnect-platform/compatibility/)
  - ID: P001
    Severity: error
    Description: |
      Kong data plane nodes running OSS versions are not supported on the Konnect
      platform and will not receive any traffic.
    Resolution: |
      Reconnect the data plane node using the Kong Enterprise version.
    DocumentationURL: |
      N/A
  - ID: P101
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `preferred_chain`, `storage_config.vault.auth_method`,
      `storage_config.vault.auth_path`, `storage_config.vault.auth_role`,
      `storage_config.vault.jwt_path` but Kong Gateway versions < 2.6.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P102
    Severity: error
    Description: |
      For the `aws-lambda` plugin, one or more of the following `config` fields are
      set: `base64_encode_body` but Kong Gateway versions < 2.6.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P103
    Severity: error
    Description: |
      For the `grpc-web` plugin, one or more of the following `config` fields are set:
      `allow_origin_header` but Kong Gateway versions < 2.6.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [gRPC Web plugin](/plugins/grpc-web/)
  - ID: P104
    Severity: error
    Description: |
      For the `request-termination` plugin, one or more of the following `config`
      fields are set: `echo`, `trigger` but Kong Gateway versions < 2.6.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.6.0.0` or above.
    DocumentationURL: |
      [Request Termination plugin](/plugins/request-termination/)
  - ID: P105
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `service_name_tag`, `status_tag`, `consumer_tag` but Kong Gateway versions <
      2.7.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P106
    Severity: error
    Description: |
      For the `datadog` plugin, distribution metric type is not supported in Kong
      gateway versions < 2.7.0.0. Distribution metrics will not be emitted by the
      gateway.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P107
    Severity: error
    Description: |
      For the `ip-restriction` plugin, one or more of the following `config` fields
      are set: `status`, `message` but Kong Gateway versions < 2.7.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [IP Restriction plugin](/plugins/ip-restriction/)
  - ID: P108
    Severity: error
    Description: |
      For the `rate-limiting` plugin, one or more of the following `config` fields are
      set: `redis_ssl`, `redis_ssl_verify`, `redis_server_name` but Kong Gateway
      versions < 2.7.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P109
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following fields: `header_type` have
      been set to one or more of the following values: `ignore` but Kong Gateway
      versions < 2.7.0.0 do not support these values. Impacted fields have been
      updated to the following default values in the data plane node: `preserve`.
      Plugin features that rely on these values may not be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P110
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `local_service_name` but Kong Gateway versions < 2.7.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P111
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `rsa_key_size` but Kong Gateway versions < 2.8.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P112
    Severity: error
    Description: |
      For the `rate-limiting` plugin, one or more of the following `config` fields are
      set: `redis_username`, `redis.username` but Kong Gateway versions < 2.8.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P113
    Severity: error
    Description: |
      For the `response-ratelimiting` plugin, one or more of the following `config`
      fields are set: `redis_username`, `redis.username` but Kong Gateway versions <
      2.8.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.0.0` or above.
    DocumentationURL: |
      [Response Rate Limiting plugin](/plugins/response-ratelimiting/)
  - ID: P115
    Severity: error
    Description: |
      Plugin `opentelemetry` is not available in Kong Gateway versions < 3.0.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P116
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `http_span_name`, `connect_timeout`, `send_timeout`, `read_timeout` but Kong
      Gateway versions < 3.0.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P117
    Severity: error
    Description: |
      For the `prometheus` plugin, one or more of the following `config` fields are
      set: `status_code_metrics`, `latency_metrics`, `bandwidth_metrics`,
      `upstream_health_metrics` but Kong Gateway versions < 3.0.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Prometheus plugin](/plugins/prometheus/)
  - ID: P118
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `allow_any_domain` but Kong Gateway versions < 3.0.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P119
    Severity: error
    Description: |
      For the `Service` entity, `enabled` field has been set to `true` but Kong
      Gateway versions < 2.7.0.0 do not support this feature. The Service has been
      left enabled in the Kong Gateway, and the traffic for the Service is being
      routed by Kong Gateway. This is a critical error and may result in unwanted
      traffic being routed to the upstream Services via Kong Gateway.
    Resolution: |
      Please upgrade Kong Gateway to version `2.7.0.0` or above.
    DocumentationURL: |
      [Services entity](/gateway/entities/service/)
  - ID: P120
    Severity: error
    Description: |
      For the `aws-lambda` plugin, one or more of the following `config` fields are
      set: `proxy_scheme` but Kong Gateway versions >= 3.0.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please use `config.proxy_url` instead of `config.proxy_scheme` field.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P121
    Severity: error
    Description: |
      For the `aws-lambda` plugin, `config.aws_region` and `config.host` fields are
      set. These fields were mutually exclusive for Kong gateway versions < 3.0.0.0.
      The plugin configuration has been changed to remove the `config.host` field.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P122
    Severity: error
    Description: |
      For the `pre-function` plugin, `config.functions` field has been used. This is
      not supported in Kong Gateway versions >= 3.0.0.0. The plugin configuration has
      been updated to rename `config.functions` to `config.access` in the data-plane.
    Resolution: |
      Please update the configuration to use `config.access` field instead of
      `config.functions`.
    DocumentationURL: |
      [Pre-function plugin](/plugins/pre-function/)
  - ID: P123
    Severity: error
    Description: |
      For the `post-function` plugin, `config.functions` field has been used. This is
      not supported in Kong Gateway versions >= 3.0.0.0. The plugin configuration has
      been updated to rename `config.functions` to `config.access` in the data-plane.
    Resolution: |
      Please update the configuration to use `config.access` field instead of
      `config.functions`.
    DocumentationURL: |
      [Post-function plugin](/plugins/post-function/)
  - ID: P124
    Severity: warning
    Description: |
      For the `pre-function` plugin, `config.functions` field has been used. This
      field is deprecated and it is no longer supported in Kong Gateway versions >=
      3.0.0.0.
    Resolution: |
      Please update the plugin configuration to use `config.access` field in place of
      `config.functions` field
    DocumentationURL: |
      [Pre-function plugin](/plugins/pre-function/)
  - ID: P125
    Severity: warning
    Description: |
      For the `post-function` plugin, `config.functions` field has been used. This
      field is deprecated and it is no longer supported in Kong Gateway versions >=
      3.0.0.0.
    Resolution: |
      Please update the plugin configuration to use `config.access` field in place of
      `config.functions` field
    DocumentationURL: |
      [Post-function plugin](/plugins/post-function/)
  - ID: P126
    Severity: error
    Description: |
      For the `upstream` entity, one or more of the following schema fields are set:
      `hash_on_query_arg`, `hash_fallback_query_arg`, `hash_on_uri_capture`,
      `hash_fallback_uri_capture` but Kong Gateway versions < 3.0.0.0 do not support
      these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Upstreams entity](/gateway/entities/upstream/)
  - ID: P127
    Severity: error
    Description: |
      For the `upstreams` entity, one or more of the `hash_on, hash_fallback` schema
      fields are set with one of the following values: path, query_arg, uri_capture,
      but Kong Gateway versions < 3.0.0.0 do not support these values. Because of
      this, `hash_on` and `hash_fallback'have been changed in the data plane node to
      `none` and hashing is not working as expected.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Upstreams entity](/gateway/entities/upstream/)
  - ID: P128
    Severity: warning
    Description: |
      For the `paths` field used in route a regex pattern usage was detected. This
      field has been back-ported by stripping the prefix `~`. Routes which rely on
      regex parsing may not work as intended on Kong gateway versions < 3.0.0.0. If
      upgrading to version 3.0.0.0 is not possible, using paths without the `~` prefix
      will avoid this warning.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: P129
    Severity: warning
    Description: |
      For the `paths` field used in route a regex pattern usage was detected. Kong
      gateway versions 3.0.0.0 and above require that regular expressions start with a
      `~` character to distinguish from simple prefix match.
    Resolution: |
      To define a regular expression based path for routing, please prefix the path
      with ~ character.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: P130
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following metrics are being used:
      `shdict_usage`, `status_count_per_user_per_route`, `status_count_per_workspace`,
      but Kong Gateway versions < 3.0.0.0 do not support these. These metrics have
      been removed from the `statsd` plugin configuration and features that rely on
      them are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P131
    Severity: error
    Description: |
      For the `statsd` plugin, one or more metrics have one or more of the following
      fields set: `workspace_identifier`, `service_identifier`. These fields are
      unsupported in Kong Gateway versions < 3.0.0.0 and have been removed. Features
      that rely on these removed fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P132
    Severity: error
    Description: |
      For the `statsd` plugin, one or more metrics don't have the
      `consumer_identifier` field set. For these metrics, the value of the
      `consumer_identifier` field has been set to `custom_id`.
    Resolution: |
      Explicitly set the `consumer_identifier` field for all metrics or upgrade Kong
      Gateway to a version >= 3.0.0.0.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P133
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `allow_status_codes`, `udp_packet_size`, `use_tcp`, `hostname_in_prefix`,
      `consumer_identifier_default`, `service_identifier_default`,
      `workspace_identifier_default` but Kong Gateway versions < 3.0.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P134
    Severity: error
    Description: |
      Tags that contain space characters (` `) are not supported on Kong gateway
      versions < 3.0.0.0. As such, all tag values that contain space characters have
      been removed.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Tags](/gateway/tags/)
  - ID: P135
    Severity: error
    Description: |
      The `vaults` entity is being used, but Kong Gateway versions < 3.1.0.0 do not
      support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P136
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `http_response_header_for_traceid` but Kong Gateway versions < 3.1.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P137
    Severity: error
    Description: |
      For the `rate-limiting` plugin, one or more of the following `config` fields are
      set: `error_code`, `error_message` but Kong Gateway versions < 3.1.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P138
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `storage_config.redis.ssl`, `storage_config.redis.ssl_verify`,
      `storage_config.redis.ssl_server_name`, `storage_config.redis.server_name` but
      Kong Gateway versions < 3.1.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P139
    Severity: error
    Description: |
      For the `response-ratelimiting` plugin, one or more of the following `config`
      fields are set: `redis_ssl`, `redis_ssl_verify`, `redis_server_name`,
      `redis.ssl`, `redis.ssl_verify`, `redis.server_name` but Kong Gateway versions <
      3.1.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Response Rate Limiting plugin](/plugins/response-ratelimiting/)
  - ID: P140
    Severity: error
    Description: |
      For the `aws-lambda` plugin, one or more of the following `config` fields are
      set: `aws_assume_role_arn`, `aws_role_session_name` but Kong Gateway versions <
      2.8.1.3 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `2.8.1.3` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P141
    Severity: error
    Description: |
      The `keys` entity is being used, but Kong Gateway versions < 3.1.0.0 do not
      support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Keys entity](/gateway/entities/key/)
  - ID: P142
    Severity: error
    Description: |
      The `key_sets` entity is being used, but Kong Gateway versions < 3.1.0.0 do not
      support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Keys entity](/gateway/entities/key/)
  - ID: P144
    Severity: error
    Description: |
      For the `pre-function` plugin, one or more of the following `config` fields are
      set: `ws_handshake`, `ws_client_frame`, `ws_upstream_frame`, `ws_close` but Kong
      Gateway versions < 3.0.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Pre-function plugin](/plugins/pre-function/)
  - ID: P145
    Severity: error
    Description: |
      For the `post-function` plugin, one or more of the following `config` fields are
      set: `ws_handshake`, `ws_client_frame`, `ws_upstream_frame`, `ws_close` but Kong
      Gateway versions < 3.0.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Post-function plugin](/plugins/post-function/)
  - ID: P146
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `enable_ipv4_common_name` but Kong Gateway versions < 3.1.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P147
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `retry_count`, `queue_size`, `flush_timeout` but Kong Gateway versions < 3.1.0.0
      do not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P148
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `cookie_persistent` but Kong Gateway versions < 3.1.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P149
    Severity: error
    Description: |
      For the `plugin` entity, one or more of the following schema fields are set:
      `instance_name` but Kong Gateway versions < 3.2.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Plugins entity](/gateway/entities/plugin/)
  - ID: P150
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `phase_duration_flavor` but Kong Gateway versions < 3.2.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P151
    Severity: error
    Description: |
      For the `upstreams` entity, the `algorithm` field is set to `latency` but Kong
      Gateway versions < 3.2.0.0 do not support this value. Because of this,
      `algorithm` has been set to the default value `round-robin` and features
      depending on the `latency` algorithm may not work as expected.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Upstreams entity](/gateway/entities/upstream/)
  - ID: P152
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `retry_count`, `queue_size`, `flush_timeout` but Kong Gateway versions < 3.1.0.0
      do not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P153
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `tag_style` but Kong Gateway versions < 3.2.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P154
    Severity: error
    Description: |
      For the `statsd` plugin, one or more default metrics have been configured with a
      non-default stat_type. This is not supported in Kong Gateway versions < 3.1.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P155
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `audience`, `absolute_timeout`, `remember_cookie_name`,
      `remember_rolling_timeout`, `remember_absolute_timeout`, `response_headers`,
      `request_headers` but Kong Gateway versions < 3.2.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P156
    Severity: warning
    Description: |
      For the `session` plugin, one or more `config` fields are set but Kong Gateway
      versions < 3.2.0.0 do not support these fields. The plugin configuration has
      been updated and these fields have been mapped to their old names:
      `rolling_timeout` => `cookie_lifetime`,
      `idling_timeout` => `cookie_idletime`,
      `stale_ttl` => `cookie_discard`,
      `cookie_same_site` => `cookie_samesite`,
      `cookie_http_only` => `cookie_httponly`
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P157
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `cookie_renew` but Kong Gateway versions >= 3.2.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please remove these fields from the plugin configuration.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P158
    Severity: warning
    Description: |
      For the `session` plugin, one or more `config` fields are set but Kong Gateway
      versions < 3.2.0.0 do not support these fields. The plugin configuration has
      been updated and these fields have been mapped to their old names:
      `remember` => `cookie_persistent`
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P159
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `remember` but Kong Gateway versions < 3.1.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please remove these fields from the plugin configuration.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P160
    Severity: warning
    Description: |
      For the `session` plugin, `config.cookie_same_site` has been set to `Default`.
      This is not supported in Kong Gateway versions < 3.2.0.0. The plugin
      configuration has been changed to the `config.cookie_same_site=Lax` in the
      data-plane (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P161
    Severity: error
    Description: |
      For the `aws-lambda` plugin, the following `config` field is set:
      aws_imds_protocol_version, but Kong Gateway versions <2.8.3.0 and >=3.0.0.0,<3.2.0.0
      do not support it. Plugin features that rely on this field are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P162
    Severity: error
    Description: |
      For the `opentelemetry` plugin, instances have been configured on a specific
      service, route, or consumer. This is not supported in Kong Gateway versions <
      3.2.0.0. The plugin has been removed in the data-plane.
    Resolution: |
      Unset the `service`, `route`, and `consumer` fields for the plugin, or upgrade
      Kong Gateway to a version >= 3.2.0.0.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P163
    Severity: error
    Description: |
      For the `graphql-rate-limiting-advanced` plugin on Kong Gateway versions
      <3.2.0.0, the `cluster` option is not supported on hybrid mode. The plugin
      instance has been removed to avoid system crashes.
    Resolution: |
      Please upgrade to Kong Gateway 3.2.0.0 or later, which supports the `cluster`
      mode with the `sync_rate = -1` option, or change to the `redis` mode.
    DocumentationURL: |
      [Graphql Rate Limiting Advanced plugin](/plugins/graphql-rate-limiting-advanced/)
  - ID: P164
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `retry_count`, `queue_size`, `flush_timeout` but Kong Gateway versions >=
      3.3.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Starting with version 3.3.0.0, the plugin `datadog` uses the field `queue`
      instead of `retry_count`, `queue_size`, `flush_timeout` used previously. Please
      update the configuration to use the new fields.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P165
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P166
    Severity: error
    Description: |
      For the `http-log` plugin, one or more of the following `config` fields are set:
      `retry_count`, `queue_size`, `flush_timeout` but Kong Gateway versions >=
      3.3.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Starting with version 3.3.0.0, the plugin `http-log` uses the field `queue`
      instead of `retry_count`, `queue_size`, `flush_timeout` used previously. Please
      update the configuration to use the new fields.
    DocumentationURL: |
      [Http Log plugin](/plugins/http-log/)
  - ID: P167
    Severity: error
    Description: |
      For the `http-log` plugin, one or more of the following `config` fields are set:
      `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Http Log plugin](/plugins/http-log/)
  - ID: P168
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `retry_count`, `queue_size`, `flush_timeout` but Kong Gateway versions >=
      3.3.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Starting with version 3.3.0.0, the plugin `statsd` uses the field `queue`
      instead of `retry_count`, `queue_size`, `flush_timeout` used previously. Please
      update the configuration to use the new fields.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P169
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P170
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `batch_span_count`, `batch_flush_delay` but Kong Gateway versions >=
      3.3.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Starting with version 3.3.0.0, the plugin `opentelemetry` uses the field `queue`
      instead of `batch_span_count`, `batch_flush_delay` used previously. Please
      update the configuration to use the new fields.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P171
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P172
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `header_type` but Kong Gateway versions >= 3.3.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P173
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `account_key`, `storage_config.redis.namespace` but Kong Gateway versions <
      3.3.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P174
    Severity: error
    Description: |
      For the `proxy-cache` plugin, one or more of the following `config` fields are
      set: `ignore_uri_case` but Kong Gateway versions < 3.3.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Proxy Cache plugin](/plugins/proxy-cache/)
  - ID: P175
    Severity: error
    Description: |
      For the `aws-lambda` plugin, one or more of the following `config` fields are
      set: `disable_https` but Kong Gateway versions < 3.3.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P176
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P177
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P178
    Severity: error
    Description: |
      For the `http-log` plugin, the `config.content_type` field has been set to
      `application/json; charset=utf-8`. This is not supported in Kong Gateway
      versions < 3.3.0.0. The plugin configuration has been changed to
      `config.content_type=application/json` in the data-plane.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Http Log plugin](/plugins/http-log/)
  - ID: P179
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `http_response_header_for_traceid` but Kong Gateway versions >= 3.3.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P180
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `queue` but Kong Gateway versions < 3.3.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P181
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following fields: `header_type`,
      `default_header_type` have been set to one or more of the following values:
      `datadog` but Kong Gateway versions < 3.0.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `preserve`, `b3`. Plugin features that rely on these values may not
      be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P182
    Severity: error
    Description: |
      For the `upstream` entity, one or more of the following schema fields are set:
      `use_srv_name` but Kong Gateway versions < 3.1.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Upstreams entity](/gateway/entities/upstream/)
  - ID: P183
    Severity: error
    Description: |
      For the `upstream` entity, one or more of the following schema fields are set:
      `healthchecks.active.headers` but Kong Gateway versions < 3.0.0.0 do not support
      these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.0.0.0` or above.
    DocumentationURL: |
      [Upstreams entity](/gateway/entities/upstream/)
  - ID: P184
    Severity: error
    Description: |
      For the `vault` entity, one or more of the following schema fields are set:
      `config.ttl`, `config.neg_ttl`, `config.resurrect_ttl` but Kong Gateway versions
      < 3.4.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P185
    Severity: error
    Description: |
      For the `rate-limiting` plugin, one or more of the following `config` fields are
      set: `sync_rate` but Kong Gateway versions < 3.4.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P186
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following fields:
      `header_type` have been set to one or more of the following values: `aws` but
      Kong Gateway versions < 3.4.0.0 do not support these values. Impacted fields
      have been updated to the following default values in the data plane node:
      `preserve`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P187
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following fields: `header_type`,
      `default_header_type` have been set to one or more of the following values:
      `aws` but Kong Gateway versions < 3.4.0.0 do not support these values. Impacted
      fields have been updated to the following default values in the data plane node:
      `preserve`, `b3`. Plugin features that rely on these values may not be working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P188
    Severity: error
    Description: |
      For the `ip-restriction` plugin, the `protocols` array contains one or both of:
      `TLS`, `TCP`. This is not supported in Kong Gateway versions < 3.4.0.0. These
      values have been removed and plugin features that rely on these values are not
      working as intended. If `TLS` and/or `TCP` were the only values, then protocols
      has been reset to the default values of [`HTTP`, `HTTPS`, `GRPC`, `GRPCS`].
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [IP Restriction plugin](/plugins/ip-restriction/)
  - ID: P189
    Severity: error
    Description: |
      Kong Gateway versions >= 3.1.0.0 and < 3.1.1.2 only support the wRPC protocol
      which has been removed from Kong Gateway >= 3.2.0.0. Konnect will be removing
      support for these releases of Kong Gateway on 2023/09/30. After this date these
      versions of Kong Gateway will no longer be able to establish connections with
      Konnect and will not receive configuration changes. You may upgrade to Kong
      Gateway >= 3.1.1.2; however Kong recommends upgrading to Kong Gateway 3.4.0.0
      LTS.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: P190
    Severity: warning
    Description: |
      For the `vaults` entity, the `prefix` value contains hyphens. This is not
      supported in Kong Gateway versions < 3.3.0.0. Hyphens have been converted to
      underscores.
    Resolution: |
      Update prefix values to use underscores instead of hyphens, or upgrade to Kong
      Gateway versions >= 3.3.0.0 which automatically convert hyphens to underscores.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P191
    Severity: error
    Description: |
      One or more certificates have expired. Resources that use an expired certificate
      are not behaving as expected. Data planes >= v3.2.0.0 will receive configuration
      updates but the data plane will fail to process them and will be `Out of Sync`
      until this is corrected. Data planes < v3.2.0.0 may be inoperable.
    Resolution: |
      Remove or replace expired certificates.
    DocumentationURL: |
      [Renew Certificate](/gateway/cp-dp-communication/#renew-a-certificate-in-konnect)
  - ID: P192
    Severity: error
    Description: |
      For the `response-ratelimiting` plugin, one or more of the following `config`
      fields are set in a reference format: `redis_password`, `redis_username` but
      Konnect only supports Vaults starting from Kong Gateway version 3.1.0.0. This
      plugin instance will cause the Kong Gateway to fail payload validation, causing
      payload updates to fail and potentially getting the data plane into an
      out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Response Rate Limiting plugin](/plugins/response-ratelimiting/)
  - ID: P193
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set
      in a reference format: `secret` but Konnect only supports Vaults starting from
      Kong Gateway version 3.1.0.0. This plugin instance will cause the Kong Gateway
      to fail payload validation, causing payload updates to fail and potentially
      getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P194
    Severity: error
    Description: |
      For the `rate-limiting` plugin, one or more of the following `config` fields are
      set in a reference format: `redis_password`, `redis_username` but Konnect only
      supports Vaults starting from Kong Gateway version 3.1.0.0. This plugin instance
      will cause the Kong Gateway to fail payload validation, causing payload updates
      to fail and potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P195
    Severity: error
    Description: |
      For the `loggly` plugin, one or more of the following `config` fields are set in
      a reference format: `key` but Konnect only supports Vaults starting from Kong
      Gateway version 3.1.0.0. This plugin instance will cause the Kong Gateway to
      fail payload validation, causing payload updates to fail and potentially getting
      the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Loggly plugin](/plugins/loggly/)
  - ID: P196
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set in a
      reference format: `account_email`, `eab_kid`, `eab_hmac_key`,
      `storage_config.redis.auth`, `storage_config.consul.token` but Konnect only
      supports Vaults starting from Kong Gateway version 3.1.0.0. This plugin instance
      will cause the Kong Gateway to fail payload validation, causing payload updates
      to fail and potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P197
    Severity: error
    Description: |
      For the `aws-lambda` plugin, one or more of the following `config` fields are
      set in a reference format: `aws_key`, `aws_secret`, `aws_assume_role_arn` but
      Konnect only supports Vaults starting from Kong Gateway version 3.1.0.0. This
      plugin instance will cause the Kong Gateway to fail payload validation, causing
      payload updates to fail and potentially getting the data plane into an
      out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P198
    Severity: error
    Description: |
      For the `azure-functions` plugin, one or more of the following `config` fields
      are set in a reference format: `apikey`, `clientid` but Konnect only supports
      Vaults starting from Kong Gateway version 3.1.0.0. This plugin instance will
      cause the Kong Gateway to fail payload validation, causing payload updates to
      fail and potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [Azure Functions plugin](/plugins/azure-functions/)
  - ID: P199
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set
      in a reference format: `host` but Kong Gateway versions < 3.3.0.0 do not support
      the reference format for these fields. This plugin instance will cause the Kong
      Gateway to fail payload validation, causing payload updates to fail and
      potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.3.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P200
    Severity: error
    Description: |
      For the `http-log` plugin, one or more of the following `config` fields are set
      in a reference format: `http_endpoint`, `headers` but Kong Gateway versions <
      3.2.0.0 do not support the reference format for these fields. This plugin
      instance will cause the Kong Gateway to fail payload validation, causing payload
      updates to fail and potentially getting the data plane into an out-of-sync
      state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.2.0.0` or above.
    DocumentationURL: |
      [Http Log plugin](/plugins/http-log/)
  - ID: P201
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set in a reference format: `headers` but Kong Gateway versions < 3.1.0.0 do not
      support the reference format for these fields. This plugin instance will cause
      the Kong Gateway to fail payload validation, causing payload updates to fail and
      potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.1.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P202
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set in a reference format: `endpoint` but Kong Gateway versions < 3.4.0.0 do not
      support the reference format for these fields. This plugin instance will cause
      the Kong Gateway to fail payload validation, causing payload updates to fail and
      potentially getting the data plane into an out-of-sync state.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P203
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following fields:
      `header_type` have been set to one or more of the following values: `datadog`
      but Kong Gateway versions < 3.4.1.0 do not support these values. Impacted fields
      have been updated to the following default values in the data plane node:
      `preserve`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.1.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P204
    Severity: error
    Description: |
      For the `acme` plugin, one or more of the following `config` fields are set:
      `storage_config.redis.scan_count` but Kong Gateway versions < 3.5.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P205
    Severity: error
    Description: |
      For the `proxy-cache` plugin, one or more of the following `config` fields are
      set: `response_headers` but Kong Gateway versions < 3.5.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [Proxy Cache plugin](/plugins/proxy-cache/)
  - ID: P206
    Severity: error
    Description: |
      For the `cors` plugin, one or more of the following `config` fields are set:
      `private_network` but Kong Gateway versions < 3.5.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [Cors plugin](/plugins/cors/)
  - ID: P207
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following fields:
      `header_type` have been set to one or more of the following values: `gcp` but
      Kong Gateway versions < 3.5.0.0 do not support these values. Impacted fields
      have been updated to the following default values in the data plane node:
      `preserve`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P208
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following fields: `header_type`,
      `default_header_type` have been set to one or more of the following values:
      `gcp` but Kong Gateway versions < 3.4.0.0 do not support these values. Impacted
      fields have been updated to the following default values in the data plane node:
      `preserve`, `b3`. Plugin features that rely on these values may not be working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P209
    Severity: error
    Description: |
      For the `vault` entity, one or more of the following schema fields are set:
      `endpoint_url`, `assume_role_arn`, `role_session_name` but Kong Gateway versions
      < 3.4.3.5 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.5` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P210
    Severity: error
    Description: |
      For the `vault` entity, the config contains one or more Azure vault entity. This
      is not supported in Kong Gateway versions < 3.4.3.5. The impacted configuration
      has been removed from the `vault` entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.5` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P211
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `read_body_for_logout` but Kong Gateway versions < 3.5.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P212
    Severity: error
    Description: |
      For the `vault` entity, one or more of the following schema fields are set:
      `kube_auth_path` but Kong Gateway versions < 3.4.3.4 do not support these
      fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.4` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P213
    Severity: warning
    Description: |
      One or more services have been configured with an underscore in the `host`
      field. This is only supported for Kong Gateway parity and it is not compliant
      with RFC0952.
    Resolution: |
      Remove underscores in services `host` field.
    DocumentationURL: |
      [Services entity](/gateway/entities/service/)
  - ID: P214
    Severity: warning
    Description: |
      The priority exceeds the value of 2^46, which may cause loss of precision in the
      Data Plane Node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: P215
    Severity: error
    Description: |
      Recursive OpenAPI specification found in the oas-validation plugin
      configuration. The `api_spec` must not have recursive elements for data plane
      nodes < 3.5.0.0; this data plane node will be inoperable.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      [OAS Validation plugin](/plugins/oas-validation/)
  - ID: P216
    Severity: error
    Description: |
      The `filter_chains` entity is being used, but Kong Gateway versions < 3.5.0.0 do
      not support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.5.0.0` or above.
    DocumentationURL: |
      N/A
  - ID: P217
    Severity: error
    Description: |
      For the `ip-restriction` plugin, instances have been configured on a specific
      consumer_group. This is not supported in Kong Gateway versions < 3.6.0.0. The
      plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [IP Restriction plugin](/plugins/ip-restriction/)
  - ID: P218
    Severity: error
    Description: |
      For the `proxy-cache` plugin, instances have been configured on a specific
      consumer_group. This is not supported in Kong Gateway versions < 3.6.0.0. The
      plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Proxy Cache plugin](/plugins/proxy-cache/)
  - ID: P219
    Severity: error
    Description: |
      For the `rate-limiting` plugin, instances have been configured on a specific
      consumer_group. This is not supported in Kong Gateway versions < 3.6.0.0. The
      plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P220
    Severity: error
    Description: |
      For the `request-termination` plugin, instances have been configured on a
      specific consumer_group. This is not supported in Kong Gateway versions <
      3.6.0.0. The plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Request Termination plugin](/plugins/request-termination/)
  - ID: P221
    Severity: error
    Description: |
      For the `acl` plugin, one or more of the following `config` fields are set:
      `include_consumer_groups` but Kong Gateway versions < 3.6.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [ACL plugin](/plugins/acl/)
  - ID: P222
    Severity: error
    Description: |
      For the `session` plugin, one or more of the following `config` fields are set:
      `sampling_rate` but Kong Gateway versions < 3.6.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P223
    Severity: error
    Description: |
      One or more filter chain entities have been configured, but wasm support is not
      enabled on one or more data plane nodes. Filter chains are not behaving as
      expected and the data plane may be inoperable.
    Resolution: |
      Enable wasm support on all data plane nodes or remove filter chain entities from
      the config.
    DocumentationURL: |
      Proxy-Wasm filter configuration
  - ID: P224
    Severity: error
    Description: |
      Plugin `ai-proxy` is not available in Kong Gateway versions < 3.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P225
    Severity: warning
    Description: |
      For the `rate-limiting` plugin, `limit_by` has been set to `consumer-group`.
      This is not supported in Kong Gateway versions < 3.6.0.0. Impacted field has
      been updated to its default value: `limit_by=consumer`
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P226
    Severity: error
    Description: |
      For the `vault` entity, one or more instances are configured with HCV approle
      settings.This is not supported in Kong Gateway versions < 3.6.0.0.The impacted
      `vault` instances have been removed from the configuration. Because of this,
      features that rely on these vaults are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P227
    Severity: error
    Description: |
      Plugin `ai-prompt-template` is not available in Kong Gateway versions < 3.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [AI Prompt Template plugin](/plugins/ai-prompt-template/)
  - ID: P228
    Severity: error
    Description: |
      Plugin `ai-prompt-decorator` is not available in Kong Gateway versions <
      3.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [AI Prompt Decorator plugin](/plugins/ai-prompt-decorator/)
  - ID: P229
    Severity: error
    Description: |
      For the `hmac-auth` plugin, the `algorithms` field contains the `hmac-sha1`
      value but FIPS-enabled Kong Gateways do not support it. This algorithm is
      cryptographically weak and should not be used.
    Resolution: |
      `hmac-sha1` should be removed from the `algorithms` array.
    DocumentationURL: |
      [HMAC Auth plugin](/plugins/hmac-auth/)
  - ID: P230
    Severity: warning
    Description: |
      For Kong Gateway version >= 3.4.0.0, configuration of rate limiting advanced
      plugin overrides on consumer groups is deprecated.
    Resolution: |
      Delete any existing rate limiting advanced plugin overrides. Optionally, you may
      recreate the rate limiting advanced plugin using the consumer group plugin
      scoping endpoint.
    DocumentationURL: |
      [Rate Limiting Advanced plugin](/plugins/rate-limiting-advanced/)
  - ID: P231
    Severity: error
    Description: |
      Plugin `ai-prompt-guard` is not available in Kong Gateway versions < 3.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [AI Prompt Guard plugin](/plugins/ai-prompt-guard/)
  - ID: P232
    Severity: error
    Description: |
      For the `key-auth` plugin, one or more of the following `config` fields are set:
      `realm` but Kong Gateway versions < 3.7.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Key Auth plugin](/plugins/key-auth/)
  - ID: P233
    Severity: error
    Description: |
      For the `basic-auth` plugin, one or more of the following `config` fields are
      set: `realm` but Kong Gateway versions < 3.6.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Basic Auth plugin](/plugins/basic-auth/)
  - ID: P234
    Severity: error
    Description: |
      Plugin `ai-request-transformer` is not available in Kong Gateway versions <
      3.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P235
    Severity: error
    Description: |
      Plugin `ai-response-transformer` is not available in Kong Gateway versions <
      3.6.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P236
    Severity: error
    Description: |
      For the `acme` plugin, one or more `config` fields are set but Kong Gateway
      versions < 3.6.0.0 do not support these fields. The plugin configuration has
      been updated and these fields have been mapped to their old names:
      `storage_config.redis.server_name` => `storage_config.redis.ssl_server_name`,
      `storage_config.redis.extra_options.namespace` => `storage_config.redis.namespace`,
      `storage_config.redis.extra_options.scan_count` => `storage_config.redis.scan_count`,
      `storage_config.redis.password` => `storage_config.redis.auth`
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
  - ID: P237
    Severity: error
    Description: |
      For the `rate-limiting` plugin, one or more `config` fields are set but Kong
      Gateway versions < 3.6.0.0 do not support these fields. The plugin configuration
      has been updated and these fields have been mapped to their old names:
      `redis.port` => `redis_port`,
      `redis.password` => `redis_password`,
      `redis.username` => `redis_username`,
      `redis.ssl` => `redis_ssl`,
      `redis.timeout` => `redis_timeout`,
      `redis.database` => `redis_database`,
      `redis.host` => `redis_host`,
      `redis.ssl_verify` => `redis_ssl_verify`,
      `redis.server_name` => `redis_server_name`
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Rate Limiting plugin](/plugins/rate-limiting/)
  - ID: P238
    Severity: error
    Description: |
      For the `response-ratelimiting` plugin, one or more `config` fields are set but
      Kong Gateway versions < 3.6.0.0 do not support these fields. The plugin
      configuration has been updated and these fields have been mapped to their old
      names:
      `redis.ssl` => `redis_ssl`,
      `redis.ssl_verify` => `redis_ssl_verify`,
      `redis.database` => `redis_database`,
      `redis.host` => `redis_host`,
      `redis.password` => `redis_password`,
      `redis.username` => `redis_username`,
      `redis.server_name` => `redis_server_name`,
      `redis.timeout` => `redis_timeout`,
      `redis.port` => `redis_port`
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [Response Rate Limiting plugin](/plugins/response-ratelimiting/)
  - ID: P240
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `propagation` but Kong Gateway versions < 3.7.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P241
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `propagation` but Kong Gateway versions < 3.7.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P242
    Severity: warning
    Description: |
      For the `ai-prompt-guard` plugin, regex prompts in the `allow_patterns` or
      `deny_patterns` fields should not exceed 50 characters. Regex prompts that
      exceed 50 characters are not supported in Kong Gateway versions <
      3.7.0.0.Resources that rely on the prompt validation may not function as
      expected, and this could cause Kong data plane configuration to become out of
      sync if not resolved.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Prompt Guard plugin](/plugins/ai-prompt-guard/)
  - ID: P243
    Severity: error
    Description: |
      For Kong Gateway version < 3.6.0.0, Konnect does not support router_flavor of
      type expressions.
    Resolution: |
      Upgrade Kong Gateway to version 3.6.0.0 or above to use expression router.
      Optionally, you may change the configured routes to not use the `expression`
      field for the routes to remain compatible with Kong Gateway versions < 3.6.0.0.
      The configuration has not been synced to the Data Plane nodes.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: P244
    Severity: error
    Description: |
      For Kong Gateway version >= 3.6.0.0 and < 3.7.0.0, routes can only be of type
      expressions, when the router_flavor is configured as expressions.
    Resolution: |
      Upgrade Kong Gateway to version 3.7.0.0 or above to use both traditional and
      expression routes. Optionally, you may change the configured routes to not use
      the `expression` field to remain compatible with Kong Gateway versions >=
      3.6.0.0 and < 3.7.0.0. The configuration has not been synced to the Data Plane
      nodes.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: P245
    Severity: error
    Description: |
      Kong Gateway with traditional/traditional_compatible router_flavor cannot have
      routes with the `expression` field in them.
    Resolution: |
      Remove routes with `expression` field or change the gateway's router_flavor
      config to `expressions` (only available on Kong Gateway versions >= 3.6.0.0).
      The configuration has not been synced to the Data Plane nodes.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: P246
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `response_streaming` but Kong Gateway versions < 3.7.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P250
    Severity: warning
    Description: |
      For the `route` entity, one or more `sni` values containing prefix or postfix
      wildcard `*` characters was detected. This is not supported in Kong gateway
      versions < 3.7.0.0. Impacted routes are not behaving as expected and the data
      plane may be inoperable.
    Resolution: |
      Remove any `sni` elements with unsupported wildcard formats, or upgrade to Kong
      version >= 3.7.0.0.
    DocumentationURL: |
      [Routes entity](/gateway/entities/route/)
  - ID: P251
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `auth.azure_use_managed_identity`, `auth.azure_client_id`,
      `auth.azure_client_secret`, `auth.azure_tenant_id` but Kong Gateway versions <
      3.7.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P252
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.auth.azure_use_managed_identity`,
      `llm.auth.azure_client_id`, `llm.auth.azure_client_secret`,
      `llm.auth.azure_tenant_id` but Kong Gateway versions < 3.7.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P253
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.auth.azure_use_managed_identity`,
      `llm.auth.azure_client_id`, `llm.auth.azure_client_secret`,
      `llm.auth.azure_tenant_id` but Kong Gateway versions < 3.7.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P254
    Severity: error
    Description: |
      For the `jwt`, the `algorithm` field has one of the following values set
      `ES512`, `PS256`, `PS384`, or `PS512`. Kong Gateway versions < 3.7.0.0 do not
      support these algorithms.These fields have been removed from the
      configuration.Features that rely on these algorithms will not work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [JWT Signer plugin](/plugins/jwt-signer/)
  - ID: P255
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.upstream_path` but Kong Gateway versions < 3.7.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P256
    Severity: error
    Description: |
      For the `ai-proxy` plugin, the config field `route_type` has been set to
      `preserve`. This is not supported in Kong Gateway versions < 3.7.0.0. The
      impacted field has been updated to `route_type=llm/v1/chat`.
    Resolution: |
      Please upgrade Kong Gateway to version `3.7.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P257
    Severity: error
    Description: |
      For the `aws-lambda` plugin, the config field `empty_arrays_mode` has been used.
      This is not supported in Kong Gateway versions older than 3.5.0.7, versions
      older than 3.6.1.7 for the 3.6.x release line and versions older than 3.7.1.2
      for the 3.7.x release line.
    Resolution: |
      Please upgrade Kong Gateway to a version that supports this field.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P258
    Severity: error
    Description: |
      Plugin `standard-webhooks` is not available in Kong Gateway versions < 3.8.0.0.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Standard Webhooks plugin](/plugins/standard-webhooks/)
  - ID: P259
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more instances are configured with
      `model.provider` field set to: `gemini` but Kong Gateway versions < 3.8.0.0 do
      not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P260
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `auth.gcp_service_account_json`, `auth.gcp_use_service_account`,
      `model.options.gemini` but Kong Gateway versions < 3.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P261
    Severity: warning
    Description: |
      For the `opentelemetry` plugin, one or more `config` fields are set but Kong
      Gateway versions < 3.8.0.0 do not support these fields. The plugin configuration
      has been updated and these fields have been mapped to their old names:
      `endpoint` => `traces_endpoint`
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P262
    Severity: warning
    Description: |
      For the `opentelemetry` plugin, the field `endpoint` has been set but this field
      has been renamed to `traces_endpoint` in Kong Gateway versions >= 3.8. To ensure
      compatibility, the field name has been updated in your config.
    Resolution: |
      Rename the field `endpoint` to `traces_endpoint` in your config.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P263
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `logs_endpoint`, `queue.concurrency_limit` but Kong Gateway versions <
      3.8.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P264
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more instances are configured
      with `llm.model.provider` field set to: `gemini` but Kong Gateway versions <
      3.8.0.0 do not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P265
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.auth.gcp_service_account_json`,
      `llm.auth.gcp_use_service_account`, `llm.model.options.gemini` but Kong Gateway
      versions < 3.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P266
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more instances are configured
      with `llm.model.provider` field set to: `gemini` but Kong Gateway versions <
      3.8.0.0 do not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P267
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.auth.gcp_service_account_json`,
      `llm.auth.gcp_use_service_account`, `llm.model.options.gemini` but Kong Gateway
      versions < 3.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P268
    Severity: error
    Description: |
      For the `ai-prompt-guard` plugin, one or more of the following `config` fields
      are set: `match_all_roles` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Prompt Guard plugin](/plugins/ai-prompt-guard/)
  - ID: P269
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.input_cost`, `model.options.output_cost` but Kong Gateway
      versions < 3.8.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P270
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.input_cost`, `llm.model.options.output_cost`
      but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P271
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.input_cost`, `llm.model.options.output_cost`
      but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P272
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `max_request_body_size` but Kong Gateway versions < 3.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P273
    Severity: error
    Description: |
      For the `ai-prompt-decorator` plugin, one or more of the following `config`
      fields are set: `max_request_body_size` but Kong Gateway versions < 3.8.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Prompt Decorator plugin](/plugins/ai-prompt-decorator/)
  - ID: P274
    Severity: error
    Description: |
      For the `ai-prompt-guard` plugin, one or more of the following `config` fields
      are set: `max_request_body_size` but Kong Gateway versions < 3.8.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Prompt Guard plugin](/plugins/ai-prompt-guard/)
  - ID: P275
    Severity: error
    Description: |
      For the `ai-prompt-template` plugin, one or more of the following `config`
      fields are set: `max_request_body_size` but Kong Gateway versions < 3.8.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Prompt Template plugin](/plugins/ai-prompt-template/)
  - ID: P276
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `max_request_body_size` but Kong Gateway versions < 3.8.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P277
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `max_request_body_size` but Kong Gateway versions < 3.8.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P279
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more instances are configured with
      `model.provider` field set to: `bedrock` but Kong Gateway versions < 3.8.0.0 do
      not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P280
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `auth.aws_access_key_id`, `auth.aws_secret_access_key`, `model.options.bedrock`
      but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P281
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more instances are configured
      with `llm.model.provider` field set to: `bedrock` but Kong Gateway versions <
      3.8.0.0 do not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P282
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.auth.aws_access_key_id`, `llm.auth.aws_secret_access_key`,
      `llm.model.options.bedrock` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P283
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more instances are configured
      with `llm.model.provider` field set to: `bedrock` but Kong Gateway versions <
      3.8.0.0 do not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P284
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.auth.aws_access_key_id`, `llm.auth.aws_secret_access_key`,
      `llm.model.options.bedrock` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P285
    Severity: error
    Description: |
      For the `aws-lambda` plugin, the config field `aws_sts_endpoint_url` has been
      used. This is not supported in Kong Gateway versions older than 3.4.3.13,
      versions older than 3.5.0.8 for the 3.5.x release line and versions older than
      3.6.1.8 for the 3.6.x release line and versions older than 3.7.1.3 for the 3.7.x
      release line.
    Resolution: |
      Please upgrade Kong Gateway to a version that supports this field.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P286
    Severity: error
    Description: |
      For the `ai-prompt-template` plugin, instances have been configured on a
      specific consumer. This is not supported in Kong Gateway versions < 3.8.0.0. The
      plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Prompt Template plugin](/plugins/ai-prompt-template/)
  - ID: P287
    Severity: error
    Description: |
      For the `ai-proxy` plugin, instances have been configured on a specific
      consumer. This is not supported in Kong Gateway versions < 3.8.0.0. The plugin
      has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P288
    Severity: error
    Description: |
      For the `ai-proxy` plugin, instances have been configured on a specific service.
      This is not supported in Kong Gateway versions < 3.8.0.0. The plugin has been
      removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P289
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, instances have been configured on a
      specific consumer. This is not supported in Kong Gateway versions < 3.8.0.0. The
      plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P290
    Severity: error
    Description: |
      For the `jwt` plugin, one or more of the following `config` fields are set:
      `realm` but Kong Gateway versions < 3.8.0.0 do not support these fields. Plugin
      features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [JWT plugin](/plugins/jwt/)
  - ID: P291
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model_name_header` but Kong Gateway versions < 3.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P292
    Severity: error
    Description: |
      For the `acl` plugin, one or more of the following `config` fields are set:
      `always_use_authenticated_groups` but Kong Gateway versions < 3.6.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.6.0.0` or above.
    DocumentationURL: |
      [ACL plugin](/plugins/acl/)
  - ID: P294
    Severity: error
    Description: |
      For the `ldap-auth` plugin, one or more of the following `config` fields are
      set: `realm` but Kong Gateway versions < 3.8.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [LDAP Authorization plugin](/plugins/ldap-auth/)
  - ID: P295
    Severity: error
    Description: |
      For the `hmac-auth` plugin, one or more of the following `config` fields are
      set: `realm` but Kong Gateway versions < 3.8.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [HMAC Auth plugin](/plugins/hmac-auth/)
  - ID: P296
    Severity: error
    Description: |
      For the `response-transformer` plugin, one or more of the following `config`
      fields are set: `rename.json` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Response Transformer plugin](/plugins/response-transformer/)
  - ID: P297
    Severity: error
    Description: |
      For the `prometheus` plugin, one or more of the following `config` fields are
      set: `ai_metrics` but Kong Gateway versions < 3.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Prometheus plugin](/plugins/prometheus/)
  - ID: P299
    Severity: error
    Description: |
      For the `http-log` plugin, one or more of the following `config` fields are set:
      `queue.concurrency_limit` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Http Log plugin](/plugins/http-log/)
  - ID: P300
    Severity: error
    Description: |
      For the `statsd` plugin, one or more of the following `config` fields are set:
      `queue.concurrency_limit` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [StatsD plugin](/plugins/statsd/)
  - ID: P301
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following `config` fields are set:
      `queue.concurrency_limit` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P302
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `queue.concurrency_limit` but Kong Gateway versions < 3.8.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P303
    Severity: error
    Description: |
      For the `vault` entity, the config field `sts_endpoint_url` has been used. This
      is not supported in Kong Gateway versions older than 3.4.3.13, versions older
      than 3.5.0.8 for the 3.5.x release line and versions older than 3.6.1.8 for the
      3.6.x release line and versions older than 3.7.1.3 for the 3.7.x release line.
    Resolution: |
      Please upgrade Kong Gateway to a version that supports this field.
    DocumentationURL: |
      [Vaults entity](/gateway/entities/vault/)
  - ID: P304
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `auth.allow_override` but Kong Gateway versions < 3.8.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P305
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `auth.allow_override` but Kong Gateway versions < 3.8.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P306
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `auth.allow_override` but Kong Gateway versions < 3.8.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.8.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P307
    Severity: error
    Description: |
      The `custom_plugins` entity is being used, but Kong Gateway versions < 3.9.0.0
      do not support this entity.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [Custom plugins](/custom-plugins/)
  - ID: P309
    Severity: error
    Description: |
      One or more streaming custom plugin entities have been configured, but streaming
      support is not enabled on the data plane node. Streaming custom plugins have
      been removed from the config and the data plane may be inoperable.
    Resolution: |
      Enable streaming custom plugin support on the data plane node or remove
      custom-plugin entities from the config.
    DocumentationURL: |
      [Custom Plugin](/custom-plugins/)
  - ID: P310
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.huggingface` but Kong Gateway versions <
      3.9.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)

  - ID: P311
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more instances are configured with
      `model.provider` field set to: `huggingface` but Kong Gateway versions < 3.9.0.0
      do not support this value. The impacted plugin has been removed from the
      configuration. Because of this, features that rely on these plugins are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)

  - ID: P312
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.huggingface` but Kong Gateway versions < 3.9.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.9.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)

  - ID: P313
    Severity: error
    Description: |
      For the `prometheus` plugin, one or more of the following `config` fields are
      set: `wasm_metrics` but Kong Gateway versions < 3.10.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Prometheus plugin](/plugins/prometheus/)

  - ID: P314
    Severity: error
    Description: |
      For the `cors` plugin, one or more of the following `config` fields are set:
      `allow_origin_absent` but Kong Gateway versions < 3.10.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [`CORS` plugin](/plugins/cors/)

  - ID: P315
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following fields:
      `header_type` have been set to one or more of the following values: `instana`
      but Kong Gateway versions < 3.10.0.0 do not support these values. Impacted
      fields have been updated to the following default values in the data plane node:
      `preserve`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P316
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following fields: `header_type` have
      been set to one or more of the following values: `instana` but Kong Gateway
      versions < 3.10.0.0 do not support these values. Impacted fields have been
      updated to the following default values in the data plane node: `preserve`.
      Plugin features that rely on these values may not be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P317
    Severity: error
    Description: |
      For the `zipkin` plugin, one or more of the following fields:
      `default_header_type` have been set to one or more of the following values:
      `instana` but Kong Gateway versions < 3.10.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `b3`. Plugin features that rely on these values may not be working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Zipkin plugin](/plugins/zipkin/)
  - ID: P318
    Severity: error
    Description: |
      For the `key-auth` plugin, one or more of the following `config` fields are set:
      `identity_realms` but Kong Gateway versions < 3.10.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Key Auth plugin](/plugins/key-auth/)
  - ID: P319
    Severity: warning
    Description: |
      For the `key` entity, one or more of the following schema fields are set: `x5t`
      but Kong Gateway versions < 3.10.0.0 do not support these fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [Key entity](/gateway/entities/key/)
  - ID: P320
    Severity: warning
    Description: |
      For the `ai-proxy`, `ai-response-transformer` or `ai-request-transformer`
      plugins, `aws_role_session_name`, `aws_sts_endpoint_url` or
      `aws_assume_role_arn` fields are set in `model.options.bedrock`. These fields
      are not supported for Kong gateway versions < 3.10.0.0. The plugin configuration
      has been changed to remove the unsupported fields.
    Resolution: |
      Please upgrade Kong Gateway to version `3.10.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P321
    Severity: error
    Description: |
      For the `jwt` credential, the `algorithm` field has been set to one of the
      following values: ESP256, ESP384, ESP512, ESB256, ESB320, ESB384, ESB512,
      ES256K, Ed25519, Ed448. This is not supported in Kong Gateway versions <
      3.11.0.0. As a result, the credential has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [JWT Signer plugin](/plugins/jwt-signer/)
  - ID: P322
    Severity: error
    Description: |
      For the `ai-prompt-decorator` plugin, one of the `content` fields in
      `config.prompts.prepend[]` or `config.prompts.append[]` exceeds 500
      characters.Kong Gateway versions < 3.11.0.0 only support values up to 500
      characters. As a result, Konnect will not sync the configuration to the data
      plane node.
    Resolution: |
      Update the value of all `content` fields to contain <= 500 characters or upgrade
      to Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Prompt Decorator plugin](/plugins/ai-prompt-decorator/)
  - ID: P323
    Severity: error
    Description: |
      For the `ai-proxy` plugin, the `protocol` field has been set to one of: `ws`,
      `wss`. This is not supported in Kong Gateway versions < 3.11.0.0. Unsupported
      protocols have been removed. If no protocols are left, the plugin has been
      removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P325
    Severity: error
    Description: |
      For the `aws-lambda` plugin, one or more of the following `config` fields are
      set: `awsgateway_compatible_payload_version` but Kong Gateway versions <
      3.12.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AWS Lambda plugin](/plugins/aws-lambda/)
  - ID: P327
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.bedrock.embeddings_normalize` but Kong Gateway versions <
      3.11.0.0 do not support these fields. Plugin features that rely on these fields
      are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P328
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.bedrock.embeddings_normalize` but Kong
      Gateway versions < 3.11.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P329
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.bedrock.embeddings_normalize` but Kong
      Gateway versions < 3.11.0.0 do not support these fields. Plugin features that
      rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P330
    Severity: warning
    Description: |
      For the `ai-prompt-decorator` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `cohere`,
      `huggingface` but Kong Gateway versions < 3.11.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `openai`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Prompt Decorator plugin](/plugins/ai-prompt-decorator/)
  - ID: P331
    Severity: warning
    Description: |
      For the `ai-prompt-guard` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `cohere`,
      `huggingface` but Kong Gateway versions < 3.11.0.0 do not support these values.
      Impacted fields have been updated to the following default values in the data
      plane node: `openai`. Plugin features that rely on these values may not be
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Prompt Guard plugin](/plugins/ai-prompt-guard/)
  - ID: P332
    Severity: warning
    Description: |
      For the `ai-proxy` plugin, one or more of the following fields: `llm_format`
      have been set to one or more of the following values: `cohere`, `huggingface`
      but Kong Gateway versions < 3.11.0.0 do not support these values. Impacted
      fields have been updated to the following default values in the data plane node:
      `openai`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P333
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `genai_category` but Kong Gateway versions < 3.11.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P334
    Severity: error
    Description: |
      For the `ai-prompt-guard` plugin, one or more of the following `config` fields
      are set: `genai_category` but Kong Gateway versions < 3.11.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Prompt Guard plugin](/plugins/ai-prompt-guard/)
  - ID: P335
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more instances are configured with
      `route_type` field set to one of: `llm/v1/assistants`, `audio/v1/audio/speech`,
      `audio/v1/audio/transcriptions`, `audio/v1/audio/translations`,
      `llm/v1/batches`, `llm/v1/embeddings`, `llm/v1/files`, `image/v1/images/edits`,
      `image/v1/images/generations`, `realtime/v1/realtime`, `llm/v1/responses` but
      Kong Gateway versions < 3.11.0.0 do not support this value. The impacted plugin
      has been removed from the configuration. Because of this, features that rely on
      these plugins are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P336
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more instances are configured with
      `llm_format` set to `bedrock/gemini` and a `route_type` set to `preserve` but
      Kong Gateway versions < 3.11.0.0 do not support this value. The impacted plugin
      has been removed from the configuration. Because of this, features that rely on
      these plugins are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P337
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.embeddings_dimensions` but Kong Gateway versions < 3.11.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P338
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.embeddings_dimensions` but Kong Gateway
      versions < 3.11.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P339
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.embeddings_dimensions` but Kong Gateway
      versions < 3.11.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P340
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.cohere` but Kong Gateway versions < 3.11.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P341
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.cohere` but Kong Gateway versions < 3.11.0.0
      do not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P342
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.model.options.cohere` but Kong Gateway versions < 3.11.0.0
      do not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P343
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `model.options.gemini.endpoint_id` but Kong Gateway versions < 3.12.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P344
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `llm.model.options.gemini.endpoint_id` but Kong Gateway versions < 3.12.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P345
    Severity: error
    Description: |
      For the `ai-proxy` plugin, one or more of the following `config` fields are set:
      `llm.model.options.gemini.endpoint_id` but Kong Gateway versions < 3.12.0.0 do
      not support these fields. Plugin features that rely on these fields are not
      working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P346
    Severity: error
    Description: |
      For the `openid-connect` plugin, one or more of the following `config` fields
      are set: `session_bind` but Kong Gateway versions < 3.12.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.12.0.0` or above.
    DocumentationURL: |
      [OpenID Connect plugin](/plugins/openid-connect/)
  - ID: P347
    Severity: error
    Description: |
      For the `basic-auth` plugin, one or more of the following `config` fields are
      set: `brute_force_protection` but Kong Gateway versions < 3.13.0.0 do not
      support these fields. Plugin features that rely on these fields are not working
      as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Basic Auth plugin](/plugins/basic-auth/)
  - ID: P349
    Severity: warning
    Description: |
      For the `ai-proxy-advanced` plugin, one or more of the following fields:
      `llm_format` have been set to one or more of the following values: `anthropic`
      but Kong Gateway versions < 3.13.0.0 do not support these values. Impacted
      fields have been updated to the following default values in the data plane node:
      `openai`. Plugin features that rely on these values may not be working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: P351
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `access_logs_endpoint` but Kong Gateway versions < 3.13.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P352
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `metrics` but Kong Gateway versions < 3.13.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P353
    Severity: error
    Description: |
      For the `opentelemetry` plugin, the `protocol` field has been set to one of:
      `ws`, `wss`, `tcp`, `tls`, `udp`, `tls_passthrough`. This is not supported in
      Kong Gateway versions < 3.13.0.0. Unsupported protocols have been removed. If no
      protocols are left, the plugin has been removed in the data plane node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P354
    Severity: error
    Description: |
      For the `datadog` plugin, one or more of the following `config` fields are set:
      `route_name_tag` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [Datadog plugin](/plugins/datadog/)
  - ID: P355
    Severity: warning
    Description: |
      For the `ai-proxy` plugin, one or more of the following fields: `llm_format`
      have been set to one or more of the following values: `anthropic` but Kong
      Gateway versions < 3.13.0.0 do not support these values. Impacted fields have
      been updated to the following default values in the data plane node: `openai`.
      Plugin features that rely on these values may not be working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy plugin](/plugins/ai-proxy/)
  - ID: P356
    Severity: error
    Description: |
      For the `ai-request-transformer` plugin, one or more of the following `config`
      fields are set: `llm.weight`, `llm.metadata`, `llm.description` but Kong Gateway
      versions < 3.13.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Request Transformer plugin](/plugins/ai-request-transformer/)
  - ID: P357
    Severity: error
    Description: |
      For the `ai-response-transformer` plugin, one or more of the following `config`
      fields are set: `llm.weight`, `llm.metadata`, `llm.description` but Kong Gateway
      versions < 3.13.0.0 do not support these fields. Plugin features that rely on
      these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Response Transformer plugin](/plugins/ai-response-transformer/)
  - ID: P358
    Severity: error
    Description: |
      For the `http-log` plugin, one or more of the following `config` fields are set:
      `ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [HTTP Log plugin](/plugins/http-log/)
  - ID: P359
    Severity: error
    Description: |
      For the `tcp-log` plugin, one or more of the following `config` fields are set:
      `ssl_verify` but Kong Gateway versions < 3.13.0.0 do not support these fields.
      Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [TCP Log plugin](/plugins/tcp-log/)
  - ID: P360
    Severity: error
    Description: |
      For the `opentelemetry` plugin, one or more of the following `config` fields are
      set: `sampling_strategy` but Kong Gateway versions < 3.11.0.0 do not support
      these fields. Plugin features that rely on these fields are not working as
      intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.11.0.0` or above.
    DocumentationURL: |
      [OpenTelemetry plugin](/plugins/opentelemetry/)
  - ID: P362
    Severity: error
    Description: |
      For the `ai-mcp-proxy` plugin, one or more of the following `config` fields are
      set: `tools[].name` but Kong Gateway versions < 3.13.0.0 do not support these
      fields. Plugin features that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: P363
    Severity: error
    Description: |
      For the `ai-mcp-proxy` plugin, a `default_acl` block is defined, but Kong
      Gateway versions < 3.13.0.0 do not support this. The ACL has been removed from
      this plugin. Because of this, features that rely on these parameters will not
      work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: P364
    Severity: error
    Description: |
      For the `ai-mcp-proxy` plugin, one or more tools have an `acl` block defined,
      but Kong Gateway versions < 3.13.0.0 do not support this. The ACLs have been
      removed from these tools. Because of this, features that rely on these
      parameters will not work as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: P365
    Severity: error
    Description: |
      For the `ai-mcp-proxy` plugin, one or more of the following `config` fields are
      set: `logging.log_audits`, `include_consumer_groups`, `consumer_identifier` but
      Kong Gateway versions < 3.13.0.0 do not support these fields. Plugin features
      that rely on these fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI MCP Proxy plugin](/plugins/ai-mcp-proxy/)
  - ID: P375
    Severity: error
    Description: |
      For the `ai-proxy-advanced` plugin, `config.balancer.algorithm` has been set to
      `least-connections`. This is not supported in Kong Gateway versions < 3.13.0.0.
      The plugin configuration has been changed to `round-robin in the data plane node
      (default value).
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Proxy Advanced plugin](/plugins/ai-proxy-advanced/)
  - ID: P376
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more instances have been
      configured with the `customCost` generic LLM provider. This is not supported in
      Kong Gateway versions < 3.13.0.0. The plugin has been removed in the data plane
      node.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: P377
    Severity: error
    Description: |
      For the `ai-rate-limiting-advanced` plugin, one or more of the following
      `config` fields are set: `custom_cost_count_function` but Kong Gateway versions
      < 3.13.0.0 do not support these fields. Plugin features that rely on these
      fields are not working as intended.
    Resolution: |
      Please upgrade Kong Gateway to version `3.13.0.0` or above.
    DocumentationURL: |
      [AI Rate Limiting Advanced plugin](/plugins/ai-rate-limiting-advanced/)
  - ID: P381
    Severity: error
    Description: |
      For the `session` plugin, the `hash_subject` and/or `store_metadata` fields have
      been set but these are unsupported in Kong Gateway versions <3.4.3.17 for the
      3.4.x series, <3.7.1.5 for the 3.7.x series, <3.8.1.1 for the 3.8.x series, or
      <3.9.1.0 for the 3.9.x series. The fields have been removed from the
      configuration.
    Resolution: |
      Please upgrade Kong Gateway to version `3.4.3.17` or above for the 3.4.x series,
      `3.7.1.5` or above for the 3.7.x series, 3.8.1.1 or above for the 3.8.x series,
      3.9.1.0 or above for the 3.9.x series or newer than `3.10.0.0`.
    DocumentationURL: |
      [Session plugin](/plugins/session/)
  - ID: P999
    Severity: warning
    Description: |
      For one or more of the following plugins `acme`, `rate-limiting`, and
      `response-ratelimiting`, instance(s) have been configured with both redis-ce
      partial(s) and old redis fields. These two sets of redis fields have conflicting
      values. The old redis fields have been updated with values from the partials in
      order to satisfy the Kong Gateway payload validation.
    Resolution: |
      The old redis fields will be deprecated in Kong 4.0. Please update the
      configuration to use the new fields.
    DocumentationURL: |
      [ACME plugin](/plugins/acme/)
      [Rate Limiting plugin](/plugins/rate-limiting/)
      [Response Rate Limiting plugin](/plugins/response-ratelimiting/)
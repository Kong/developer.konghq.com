title: Validate all topics against a Confluent Schema Registry

description: Ensure that every consumed topic is validated against a Confluent Schema Registry, and mark any messages that don't conform.

requirements:
  - A [schema registry](/event-gateway/entities/schema-registry/) configured in the {{site.event_gateway_short}} control plane.

weight: 900

name: validate-all-topics
type: schema_validation
config:
  type: confluent_schema_registry
  schema_registry:
    name: my-schema-registry
  key_validation_action: mark
  value_validation_action: mark

tools:
  - konnect-api
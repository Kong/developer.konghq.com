title: Validate all topics against JSON

description: Ensure that every produced topic is validated against an inferred JSON schema.

weight: 900

name: validate-all-topics
type: schema_validation
config:
  type: json
  key_validation_action: mark
  value_validation_action: mark

tools:
  - konnect-api

{
  "properties": {
    "protocols": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "grpc",
          "grpcs",
          "http",
          "https",
          "ws",
          "wss"
        ]
      },
      "description": "A list of the request protocols that will trigger this policy. The default value, as well as the possible values allowed on this field, may change depending on the policy type. For example, policies that only work in stream mode will only support tcp and tls.",
      "default": [
        "grpc",
        "grpcs",
        "http",
        "https",
        "ws",
        "wss"
      ]
    },
    "model": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        }
      },
      "description": "Custom type for representing a foreign key with a null value allowed.",
      "x-foreign": true
    },
    "config": {
      "type": "object",
      "properties": {
        "anonymous": {
          "type": "string",
          "description": "An optional string (consumer UUID or username) value to use as an “anonymous” consumer if authentication fails. If empty (default null), the request will fail with an authentication failure `4xx`. Note that this value must refer to the consumer `id` or `username` attribute, and **not** its `custom_id`."
        },
        "key_in_header": {
          "type": "boolean",
          "description": "If enabled (default), the policy reads the request header and tries to find the key in it.",
          "default": true
        },
        "key_in_query": {
          "type": "boolean",
          "description": "If enabled (default), the policy reads the query parameter in the request and tries to find the key in it.",
          "default": true
        },
        "key_in_body": {
          "type": "boolean",
          "description": "If enabled, the policy reads the request body (if said request has one and its MIME type is supported) and tries to find the key in it. Supported MIME types: `application/www-form-urlencoded`, `application/json`, and `multipart/form-data`.",
          "default": false
        },
        "run_on_preflight": {
          "type": "boolean",
          "description": "A boolean value that indicates whether the policy should run (and try to authenticate) on `OPTIONS` preflight requests. If set to `false`, then `OPTIONS` requests are always allowed.",
          "default": true
        },
        "realm": {
          "type": "string",
          "description": "When authentication fails the policy sends `WWW-Authenticate` header with `realm` attribute value."
        },
        "key_names": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "A string representing an HTTP header name."
          },
          "description": "Describes an array of parameter names where the policy will look for a key. The client must send the authentication key in one of those key names, and the policy will try to read the credential from a header, request body, or query string parameter with the same name.  Key names may only contain [a-z], [A-Z], [0-9], [_] underscore, and [-] hyphen.",
          "default": [
            "apikey"
          ]
        },
        "hide_credentials": {
          "type": "boolean",
          "description": "An optional boolean value telling the policy to show or hide the credential from the upstream service. If `true`, the policy strips the credential from the request (i.e., the header, query string, or request body containing the key) before proxying it.",
          "default": true
        }
      }
    },
    "route": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "description": "If set, the policy will only activate when receiving requests via the specified route. Leave unset for the policy to activate regardless of the route being used."
    },
    "service": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "description": "If set, the policy will only activate when receiving requests via one of the routes belonging to the specified Service. Leave unset for the policy to activate regardless of the Service being matched."
    }
  }
}
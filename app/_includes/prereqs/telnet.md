`telnet` is a CLI that lets you open raw TCP connections to a host and port.
This tutorial requires `telnet` for validation.

macOS doesn't ship `telnet`. Install it with [Homebrew](https://brew.sh/):

```sh
brew install telnet
```

On Debian and Ubuntu, install the `telnet` package:

```sh
sudo apt-get install telnet
```

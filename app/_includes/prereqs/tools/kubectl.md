1. Install [`kubectl`](https://kubernetes.io/docs/tasks/tools/), the Kubernetes command-line tool.

1. Confirm that `kubectl` points at the cluster you want to use:

   ```sh
   kubectl config current-context
   ```

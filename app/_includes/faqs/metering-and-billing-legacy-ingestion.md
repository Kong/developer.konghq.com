Your existing built-in setup continues to work, but the button is no longer available to new users. 
If you want to migrate to the Metering & Billing plugin, disable the built-in integration first to avoid event duplication, then follow the [plugin setup instructions](/plugins/metering-and-billing/examples/).

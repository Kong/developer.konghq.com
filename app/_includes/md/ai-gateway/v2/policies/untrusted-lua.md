{:.warning}
> **Warning:** The Pre-function and Post-function Policies allow anyone who can create the Policy to execute arbitrary code.
If your organization has security concerns about this, [disable the Policies](/gateway/configuration/#untrusted-lua) in your `kong.conf` file.

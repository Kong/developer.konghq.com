{:.warning}
> **Legacy built-in event ingestion:** If you previously enabled metering using the built-in {{site.konnect_short_name}} UI, do **not** enable the Metering & Billing plugin at the same time. This can result in duplicate events. Disable the built-in event ingestion first, then enable the plugin.

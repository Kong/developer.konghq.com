```bash
Usage:
  kongctl version [flags]

Examples:
  # Print the simple version
  kongctl version
  # Print the full version info with commit and build date
  kongctl version --full

Flags:
      --full   True to show the full version information.
                (config path = 'version.full')
  -h, --help   help for version

```
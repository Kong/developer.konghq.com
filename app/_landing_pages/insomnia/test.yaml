metadata:
  title: Test APIs
rows:
  - header:
      type: h1
      text: "Test APIs with Insomnia"
    columns:
      - blocks:
          - type: structured_text
            config:
              header:
                text: "Test APIs and write scripts in Insomnia"
              blocks:
                - type: text
                  text: | 
                    Insomnia provides several tools you can use to test APIs. 
      - blocks:
          - type: image
            config:
              url: /assets/images/insomnia/graphic-api-test-hero.png
              alt_text: Image of the generating collections and tests         

  - header:
      type: h2
      text: "Get started"
    columns:
      - blocks:
        - type: card
          config:
            title: Organize and run your API tests
            description: Write customizable Javascript tests directly in Insomnia. Organize your integration tests as needed and run them simultaneously to identify errors.
            icon: /assets/icons/insomnia/checkbox-active.svg
            cta:
              text: Learn more
              url: 
              align: end
      - blocks:
        - type: card
          config:
            title: Chain requests
            description: Insomnia provides the ability to chain requests. This means you can extract values from the responses of other requests while also sharing state across requests.
            icon: /assets/icons/insomnia/icon-transfer-light-02.svg
            cta:
              text: Learn more
              url: 
              align: end
      - blocks:
        - type: card
          config:
            title: Automate API test execution with CLI
            description: Use the Insomnia CLI to automate the execution of your API tests using your existing Git workflows. For example, you can make it so that every pull request needs to run the API tests successfully before they can be merged.
            icon: /assets/icons/insomnia/icon-omnichannel-light-02.svg
            cta:
              text: Learn more
              url: 
              align: end 
      - blocks:
        - type: card
          config:
            title: Use Collection Runner 
            description: Automatically test multiple requests in your collections, with unlimited collection runs.
            icon: /assets/icons/insomnia/icon-innovation-light-02.svg
            cta:
              text: Learn more
              url: 
              align: end  

  - header:
      type: h2
      text: Learn
    columns:
      - blocks:
          - type: concept_list
            config:
              - topic: test-apis
                product: insomnia 
            quantity: 5
            allow_empty: true
          - type: cta
            config:
              text: "Learn more &raquo;"
              url: "https://docs.devsite.com/search/{concept.topic}+{other stuff}" ## Takes you to search with terms that match this.
              align: end

  - header:
      type: h2
      text: How-to Guides
    columns:
      - blocks:
          - type: how_to_list
            config:
              products:
                  - insomnia
              quantity: 5
              allow_empty: true
              view_more: "https://docs.devsite.com/search/{topic}+{other stuff}" ## Takes you to search with terms that match this.
  
  - header:
      type: h2
      text: References & Tools
    columns:
      - blocks:
          - type: reference_list
            config:
              products:
                  - insomnia 
            quantity: 5
            allow_empty: true
          - type: cta
            config:
              text: "View more references &raquo;"
              url: "https://docs.devsite.com/search/{reference.topic}+{other stuff}" ## Takes you to search with terms that match this.
              align: end
      
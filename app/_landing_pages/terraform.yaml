metadata:
  title: Terraform
  product: terraform
rows:
  - header:
      type: h1
      text: "Terraform"

  - columns:
      - blocks:
          - type: structured_text
            config:
              header:
                text: "Terraform"
              blocks:
                - type: text
                  text: |
                    Kong provides [terraform-provider-konnect](https://github.com/Kong/terraform-provider-konnect), which allows you to manage your Konnect resources using Terraform.  
     
      - blocks:
          - type: mermaid
            config:
              alt_text: "A diagram that explains how Terraform works with Konnect"
              diagram: |
                sequenceDiagram
                participant User
                participant Terraform
                participant Konnect API
                participant Konnect
                participant Kong Gateway
                participant Application

                User->>Terraform: Write configuration code
                Terraform->>Konnect API: Send configuration instructions
                Konnect API->>Konnect: Apply configuration
                Konnect->>Kong Gateway: Configure Kong Gateway
                Kong Gateway->>Application: Manage and handle application traffic
  
  - header:
      type: h2
      text: How-to Guides
    columns:
      - blocks:
          - type: how_to_list
            config:
              products:
                  - terraform
              quantity: 5
              allow_empty: true
              view_more: "https://docs.devsite.com/search/{topic}+{other stuff}" ## Takes you to search with terms that match this.
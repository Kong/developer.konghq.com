metadata:
  title: "{{site.base_gateway}} entities"
  content_type: landing_page
  description: This page lists the different entities that make up {{site.base_gateway}}.
  breadcrumbs:
    - /gateway/
  products:
    - gateway
  works_on:
    - on-prem
    - konnect
rows:
  -  header:
        type: h1
        text: "{{site.base_gateway}} entities"
        sub_text: "Entities are the components and objects that make up the Kong API Gateway and its ecosystem"

  - header:
      type: h2
      text: "Core entities"
    column_count: 3
    columns:
      - blocks:
        - type: entity_card
          config:
            entity: service
      - blocks:
        - type: entity_card
          config:
            entity: route
      - blocks:
        - type: entity_card
          config:
            entity: consumer
      - blocks:
        - type: entity_card
          config:
            entity: consumer-group
      - blocks:
        - type: entity_card
          config:
            entity: upstream
      - blocks:
        - type: entity_card
          config:
            entity: target
      - blocks:
        - type: entity_card
          config:
            entity: plugin
      - blocks:
        - type: entity_card
          config:
            entity: workspace
      - blocks:
        - type: entity_card
          config:
            entity: partial

  - header:
      type: h2
      text: "Security"
    column_count: 3
    columns:
      - blocks:
        - type: entity_card
          config:
            entity: sni
      - blocks:
        - type: entity_card
          config:
            entity: certificate
      - blocks:
        - type: entity_card
          config:
            entity: ca_certificate
      - blocks:
        - type: entity_card
          config:
            entity: vault
      - blocks:
        - type: entity_card
          config:
            entity: key
      - blocks:
        - type: entity_card
          config:
            entity: key-set
